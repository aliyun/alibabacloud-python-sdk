# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List

from alibabacloud_sddp20190103 import models as main_models
from darabonba.model import DaraModel

class DescribeOssObjectDetailV2ResponseBody(DaraModel):
    def __init__(
        self,
        oss_object_detail: main_models.DescribeOssObjectDetailV2ResponseBodyOssObjectDetail = None,
        request_id: str = None,
    ):
        # Detailed information about the OSS storage object.
        self.oss_object_detail = oss_object_detail
        # The ID of this call request, which is a unique identifier generated by Alibaba Cloud for the request and can be used to troubleshoot and locate issues.
        self.request_id = request_id

    def validate(self):
        if self.oss_object_detail:
            self.oss_object_detail.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.oss_object_detail is not None:
            result['OssObjectDetail'] = self.oss_object_detail.to_map()

        if self.request_id is not None:
            result['RequestId'] = self.request_id

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('OssObjectDetail') is not None:
            temp_model = main_models.DescribeOssObjectDetailV2ResponseBodyOssObjectDetail()
            self.oss_object_detail = temp_model.from_map(m.get('OssObjectDetail'))

        if m.get('RequestId') is not None:
            self.request_id = m.get('RequestId')

        return self

class DescribeOssObjectDetailV2ResponseBodyOssObjectDetail(DaraModel):
    def __init__(
        self,
        bucket_name: str = None,
        category_name: str = None,
        file_category_name: str = None,
        id: str = None,
        model_tag_ids: str = None,
        name: str = None,
        object_acl: str = None,
        region_id: str = None,
        risk_level_id: int = None,
        risk_level_name: str = None,
        rule_list: List[main_models.DescribeOssObjectDetailV2ResponseBodyOssObjectDetailRuleList] = None,
        size: int = None,
    ):
        # The name of the Bucket to which the OSS storage object belongs.
        self.bucket_name = bucket_name
        # The major category of the model.
        self.category_name = category_name
        # File type.
        self.file_category_name = file_category_name
        # The unique ID of the OSS object.
        self.id = id
        # Data labels, separated by commas. Values:
        # - **101**: Personal Sensitive Information.
        # - **102**: Personal Information.
        # - **107**: General Information.
        self.model_tag_ids = model_tag_ids
        # OSS storage object name.
        self.name = name
        # OSS Object ACL
        self.object_acl = object_acl
        # The region ID to which the OSS storage object belongs.
        self.region_id = region_id
        # The sensitivity level of the OSS object. Valid values:
        # 
        # *   **1**: N/A, which indicates that no sensitive data is detected.
        # *   **2**: S1, which indicates the low sensitivity level.
        # *   **3**: S2, which indicates the medium sensitivity level.
        # *   **4**: S3, which indicates the high sensitivity level.
        # *   **5**: S4, which indicates the highest sensitivity level.
        self.risk_level_id = risk_level_id
        # The risk level name of the OSS storage object.
        self.risk_level_name = risk_level_name
        # A list of sensitive data recognition rules hit by the OSS storage object.
        self.rule_list = rule_list
        # File size. Unit: Byte.
        self.size = size

    def validate(self):
        if self.rule_list:
            for v1 in self.rule_list:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.bucket_name is not None:
            result['BucketName'] = self.bucket_name

        if self.category_name is not None:
            result['CategoryName'] = self.category_name

        if self.file_category_name is not None:
            result['FileCategoryName'] = self.file_category_name

        if self.id is not None:
            result['Id'] = self.id

        if self.model_tag_ids is not None:
            result['ModelTagIds'] = self.model_tag_ids

        if self.name is not None:
            result['Name'] = self.name

        if self.object_acl is not None:
            result['ObjectAcl'] = self.object_acl

        if self.region_id is not None:
            result['RegionId'] = self.region_id

        if self.risk_level_id is not None:
            result['RiskLevelId'] = self.risk_level_id

        if self.risk_level_name is not None:
            result['RiskLevelName'] = self.risk_level_name

        result['RuleList'] = []
        if self.rule_list is not None:
            for k1 in self.rule_list:
                result['RuleList'].append(k1.to_map() if k1 else None)

        if self.size is not None:
            result['Size'] = self.size

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('BucketName') is not None:
            self.bucket_name = m.get('BucketName')

        if m.get('CategoryName') is not None:
            self.category_name = m.get('CategoryName')

        if m.get('FileCategoryName') is not None:
            self.file_category_name = m.get('FileCategoryName')

        if m.get('Id') is not None:
            self.id = m.get('Id')

        if m.get('ModelTagIds') is not None:
            self.model_tag_ids = m.get('ModelTagIds')

        if m.get('Name') is not None:
            self.name = m.get('Name')

        if m.get('ObjectAcl') is not None:
            self.object_acl = m.get('ObjectAcl')

        if m.get('RegionId') is not None:
            self.region_id = m.get('RegionId')

        if m.get('RiskLevelId') is not None:
            self.risk_level_id = m.get('RiskLevelId')

        if m.get('RiskLevelName') is not None:
            self.risk_level_name = m.get('RiskLevelName')

        self.rule_list = []
        if m.get('RuleList') is not None:
            for k1 in m.get('RuleList'):
                temp_model = main_models.DescribeOssObjectDetailV2ResponseBodyOssObjectDetailRuleList()
                self.rule_list.append(temp_model.from_map(k1))

        if m.get('Size') is not None:
            self.size = m.get('Size')

        return self

class DescribeOssObjectDetailV2ResponseBodyOssObjectDetailRuleList(DaraModel):
    def __init__(
        self,
        category_name: str = None,
        count: int = None,
        model_tags: List[main_models.DescribeOssObjectDetailV2ResponseBodyOssObjectDetailRuleListModelTags] = None,
        risk_level_id: int = None,
        risk_level_name: str = None,
        rule_name: str = None,
    ):
        # The major category of the model.
        self.category_name = category_name
        # The number of times the sensitive data recognition rule was hit.
        self.count = count
        # List of data tags.
        self.model_tags = model_tags
        # The risk level ID of the OSS storage object.
        # - **1**: No sensitive data detected.
        # - **2**: Level 1 sensitive data.
        # - **3**: Level 2 sensitive data.
        # - **4**: Level 3 sensitive data.
        # - **5**: Level 4 sensitive data.
        self.risk_level_id = risk_level_id
        # The risk level name of the OSS storage object.
        self.risk_level_name = risk_level_name
        # The name of the sensitive data recognition rule that was hit.
        self.rule_name = rule_name

    def validate(self):
        if self.model_tags:
            for v1 in self.model_tags:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.category_name is not None:
            result['CategoryName'] = self.category_name

        if self.count is not None:
            result['Count'] = self.count

        result['ModelTags'] = []
        if self.model_tags is not None:
            for k1 in self.model_tags:
                result['ModelTags'].append(k1.to_map() if k1 else None)

        if self.risk_level_id is not None:
            result['RiskLevelId'] = self.risk_level_id

        if self.risk_level_name is not None:
            result['RiskLevelName'] = self.risk_level_name

        if self.rule_name is not None:
            result['RuleName'] = self.rule_name

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('CategoryName') is not None:
            self.category_name = m.get('CategoryName')

        if m.get('Count') is not None:
            self.count = m.get('Count')

        self.model_tags = []
        if m.get('ModelTags') is not None:
            for k1 in m.get('ModelTags'):
                temp_model = main_models.DescribeOssObjectDetailV2ResponseBodyOssObjectDetailRuleListModelTags()
                self.model_tags.append(temp_model.from_map(k1))

        if m.get('RiskLevelId') is not None:
            self.risk_level_id = m.get('RiskLevelId')

        if m.get('RiskLevelName') is not None:
            self.risk_level_name = m.get('RiskLevelName')

        if m.get('RuleName') is not None:
            self.rule_name = m.get('RuleName')

        return self

class DescribeOssObjectDetailV2ResponseBodyOssObjectDetailRuleListModelTags(DaraModel):
    def __init__(
        self,
        id: int = None,
        name: str = None,
    ):
        # ID of the data label for the recognition model.
        # - **101**: Personal sensitive information.
        # - **102**: Personal information.
        # - **103**: Important data.
        self.id = id
        # Name of the data label for the recognition model.
        # - Personal sensitive information.
        # - Personal information.
        # - Important data.
        self.name = name

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.id is not None:
            result['Id'] = self.id

        if self.name is not None:
            result['Name'] = self.name

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Id') is not None:
            self.id = m.get('Id')

        if m.get('Name') is not None:
            self.name = m.get('Name')

        return self

