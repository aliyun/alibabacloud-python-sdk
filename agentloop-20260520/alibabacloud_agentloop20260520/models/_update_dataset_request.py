# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List, Dict

from alibabacloud_agentloop20260520 import models as main_models
from darabonba.model import DaraModel

class UpdateDatasetRequest(DaraModel):
    def __init__(
        self,
        description: str = None,
        renames: List[main_models.UpdateDatasetRequestRenames] = None,
        schema: Dict[str, main_models.IndexKey] = None,
        client_token: str = None,
    ):
        # The description of the dataset.
        self.description = description
        # The field renames for the dataset.
        self.renames = renames
        # The table schema of the dataset.
        self.schema = schema
        # The idempotency token. A unique string generated by the client to ensure that the update operation is idempotent.
        self.client_token = client_token

    def validate(self):
        if self.renames:
            for v1 in self.renames:
                 if v1:
                    v1.validate()
        if self.schema:
            for v1 in self.schema.values():
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.description is not None:
            result['description'] = self.description

        result['renames'] = []
        if self.renames is not None:
            for k1 in self.renames:
                result['renames'].append(k1.to_map() if k1 else None)

        result['schema'] = {}
        if self.schema is not None:
            for k1, v1 in self.schema.items():
                result['schema'][k1] = v1.to_map() if v1 else None

        if self.client_token is not None:
            result['clientToken'] = self.client_token

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('description') is not None:
            self.description = m.get('description')

        self.renames = []
        if m.get('renames') is not None:
            for k1 in m.get('renames'):
                temp_model = main_models.UpdateDatasetRequestRenames()
                self.renames.append(temp_model.from_map(k1))

        self.schema = {}
        if m.get('schema') is not None:
            for k1, v1 in m.get('schema').items():
                temp_model = main_models.IndexKey()
                self.schema[k1] = temp_model.from_map(v1)

        if m.get('clientToken') is not None:
            self.client_token = m.get('clientToken')

        return self

class UpdateDatasetRequestRenames(DaraModel):
    def __init__(
        self,
        new_name: str = None,
        old_name: str = None,
    ):
        # The new field name.
        self.new_name = new_name
        # The original field name.
        self.old_name = old_name

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.new_name is not None:
            result['newName'] = self.new_name

        if self.old_name is not None:
            result['oldName'] = self.old_name

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('newName') is not None:
            self.new_name = m.get('newName')

        if m.get('oldName') is not None:
            self.old_name = m.get('oldName')

        return self

