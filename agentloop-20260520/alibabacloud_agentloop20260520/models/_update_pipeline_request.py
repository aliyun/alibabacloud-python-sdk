# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List, Dict, Any

from alibabacloud_agentloop20260520 import models as main_models
from darabonba.model import DaraModel

class UpdatePipelineRequest(DaraModel):
    def __init__(
        self,
        description: str = None,
        execute_policy: main_models.UpdatePipelineRequestExecutePolicy = None,
        pipeline: main_models.UpdatePipelineRequestPipeline = None,
        sink: main_models.UpdatePipelineRequestSink = None,
        source: main_models.UpdatePipelineRequestSource = None,
        client_token: str = None,
    ):
        # The description of the pipeline, which helps users understand its purpose.
        self.description = description
        # The scheduling policy. If provided, the entire scheduling policy is overwritten.
        self.execute_policy = execute_policy
        # The pipeline configuration (node orchestration). If specified, the existing pipeline configuration is entirely overwritten.
        self.pipeline = pipeline
        # The pipeline sink (data write destination). If provided, the entire sink configuration is overwritten.
        self.sink = sink
        # The pipeline data source. If specified, the existing source configuration is entirely overwritten.
        self.source = source
        # The idempotency token. A unique string generated by the client to ensure the idempotency of the update operation.
        self.client_token = client_token

    def validate(self):
        if self.execute_policy:
            self.execute_policy.validate()
        if self.pipeline:
            self.pipeline.validate()
        if self.sink:
            self.sink.validate()
        if self.source:
            self.source.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.description is not None:
            result['description'] = self.description

        if self.execute_policy is not None:
            result['executePolicy'] = self.execute_policy.to_map()

        if self.pipeline is not None:
            result['pipeline'] = self.pipeline.to_map()

        if self.sink is not None:
            result['sink'] = self.sink.to_map()

        if self.source is not None:
            result['source'] = self.source.to_map()

        if self.client_token is not None:
            result['clientToken'] = self.client_token

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('description') is not None:
            self.description = m.get('description')

        if m.get('executePolicy') is not None:
            temp_model = main_models.UpdatePipelineRequestExecutePolicy()
            self.execute_policy = temp_model.from_map(m.get('executePolicy'))

        if m.get('pipeline') is not None:
            temp_model = main_models.UpdatePipelineRequestPipeline()
            self.pipeline = temp_model.from_map(m.get('pipeline'))

        if m.get('sink') is not None:
            temp_model = main_models.UpdatePipelineRequestSink()
            self.sink = temp_model.from_map(m.get('sink'))

        if m.get('source') is not None:
            temp_model = main_models.UpdatePipelineRequestSource()
            self.source = temp_model.from_map(m.get('source'))

        if m.get('clientToken') is not None:
            self.client_token = m.get('clientToken')

        return self

class UpdatePipelineRequestSource(DaraModel):
    def __init__(
        self,
        dataset: main_models.UpdatePipelineRequestSourceDataset = None,
        input_fields: List[main_models.UpdatePipelineRequestSourceInputFields] = None,
        logstore: main_models.UpdatePipelineRequestSourceLogstore = None,
        type: str = None,
    ):
        # The dataset datasource config within the current AgentSpace.
        self.dataset = dataset
        # The input fields and their types. This parameter applies to all data source types.
        self.input_fields = input_fields
        # The SLS Logstore datasource config.
        self.logstore = logstore
        # The data source type. Valid values: logstore and dataset.
        self.type = type

    def validate(self):
        if self.dataset:
            self.dataset.validate()
        if self.input_fields:
            for v1 in self.input_fields:
                 if v1:
                    v1.validate()
        if self.logstore:
            self.logstore.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.dataset is not None:
            result['dataset'] = self.dataset.to_map()

        result['inputFields'] = []
        if self.input_fields is not None:
            for k1 in self.input_fields:
                result['inputFields'].append(k1.to_map() if k1 else None)

        if self.logstore is not None:
            result['logstore'] = self.logstore.to_map()

        if self.type is not None:
            result['type'] = self.type

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('dataset') is not None:
            temp_model = main_models.UpdatePipelineRequestSourceDataset()
            self.dataset = temp_model.from_map(m.get('dataset'))

        self.input_fields = []
        if m.get('inputFields') is not None:
            for k1 in m.get('inputFields'):
                temp_model = main_models.UpdatePipelineRequestSourceInputFields()
                self.input_fields.append(temp_model.from_map(k1))

        if m.get('logstore') is not None:
            temp_model = main_models.UpdatePipelineRequestSourceLogstore()
            self.logstore = temp_model.from_map(m.get('logstore'))

        if m.get('type') is not None:
            self.type = m.get('type')

        return self

class UpdatePipelineRequestSourceLogstore(DaraModel):
    def __init__(
        self,
        logstore: str = None,
        project: str = None,
        query: str = None,
    ):
        # The name of the SLS Logstore.
        self.logstore = logstore
        # The name of the SLS project.
        self.project = project
        # The data filtered query statement in SLS query/analysis syntax.
        self.query = query

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.logstore is not None:
            result['logstore'] = self.logstore

        if self.project is not None:
            result['project'] = self.project

        if self.query is not None:
            result['query'] = self.query

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('logstore') is not None:
            self.logstore = m.get('logstore')

        if m.get('project') is not None:
            self.project = m.get('project')

        if m.get('query') is not None:
            self.query = m.get('query')

        return self

class UpdatePipelineRequestSourceInputFields(DaraModel):
    def __init__(
        self,
        name: str = None,
        type: str = None,
    ):
        # The field name.
        self.name = name
        # The field type. Valid values: text, long, double, and json.
        self.type = type

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.name is not None:
            result['name'] = self.name

        if self.type is not None:
            result['type'] = self.type

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('name') is not None:
            self.name = m.get('name')

        if m.get('type') is not None:
            self.type = m.get('type')

        return self

class UpdatePipelineRequestSourceDataset(DaraModel):
    def __init__(
        self,
        dataset: str = None,
        filter: str = None,
    ):
        # The name of the source dataset.
        self.dataset = dataset
        # The filter condition for the dataset data.
        self.filter = filter

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.dataset is not None:
            result['dataset'] = self.dataset

        if self.filter is not None:
            result['filter'] = self.filter

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('dataset') is not None:
            self.dataset = m.get('dataset')

        if m.get('filter') is not None:
            self.filter = m.get('filter')

        return self

class UpdatePipelineRequestSink(DaraModel):
    def __init__(
        self,
        condition: main_models.UpdatePipelineRequestSinkCondition = None,
        dataset: main_models.UpdatePipelineRequestSinkDataset = None,
        type: str = None,
    ):
        # The conditional routing configuration. This parameter takes effect only when sink.type is set to condition.
        self.condition = condition
        # The destination dataset configuration for the dataset sink. This parameter takes effect only when sink.type is set to dataset.
        self.dataset = dataset
        # The sink type. Valid values: dataset and condition.
        self.type = type

    def validate(self):
        if self.condition:
            self.condition.validate()
        if self.dataset:
            self.dataset.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.condition is not None:
            result['condition'] = self.condition.to_map()

        if self.dataset is not None:
            result['dataset'] = self.dataset.to_map()

        if self.type is not None:
            result['type'] = self.type

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('condition') is not None:
            temp_model = main_models.UpdatePipelineRequestSinkCondition()
            self.condition = temp_model.from_map(m.get('condition'))

        if m.get('dataset') is not None:
            temp_model = main_models.UpdatePipelineRequestSinkDataset()
            self.dataset = temp_model.from_map(m.get('dataset'))

        if m.get('type') is not None:
            self.type = m.get('type')

        return self

class UpdatePipelineRequestSinkDataset(DaraModel):
    def __init__(
        self,
        agent_space: str = None,
        dataset: str = None,
    ):
        # The name of the AgentSpace to which the destination dataset belongs.
        self.agent_space = agent_space
        # The name of the destination dataset.
        self.dataset = dataset

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.agent_space is not None:
            result['agentSpace'] = self.agent_space

        if self.dataset is not None:
            result['dataset'] = self.dataset

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('agentSpace') is not None:
            self.agent_space = m.get('agentSpace')

        if m.get('dataset') is not None:
            self.dataset = m.get('dataset')

        return self

class UpdatePipelineRequestSinkCondition(DaraModel):
    def __init__(
        self,
        default_sink: main_models.UpdatePipelineRequestSinkConditionDefaultSink = None,
        match_mode: str = None,
        routes: List[main_models.UpdatePipelineRequestSinkConditionRoutes] = None,
    ):
        # The default write destination used when no conditional route is matched.
        self.default_sink = default_sink
        # The route matching mode. Currently, only all is supported.
        self.match_mode = match_mode
        # The list of conditional routes.
        self.routes = routes

    def validate(self):
        if self.default_sink:
            self.default_sink.validate()
        if self.routes:
            for v1 in self.routes:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.default_sink is not None:
            result['defaultSink'] = self.default_sink.to_map()

        if self.match_mode is not None:
            result['matchMode'] = self.match_mode

        result['routes'] = []
        if self.routes is not None:
            for k1 in self.routes:
                result['routes'].append(k1.to_map() if k1 else None)

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('defaultSink') is not None:
            temp_model = main_models.UpdatePipelineRequestSinkConditionDefaultSink()
            self.default_sink = temp_model.from_map(m.get('defaultSink'))

        if m.get('matchMode') is not None:
            self.match_mode = m.get('matchMode')

        self.routes = []
        if m.get('routes') is not None:
            for k1 in m.get('routes'):
                temp_model = main_models.UpdatePipelineRequestSinkConditionRoutes()
                self.routes.append(temp_model.from_map(k1))

        return self

class UpdatePipelineRequestSinkConditionRoutes(DaraModel):
    def __init__(
        self,
        expression: str = None,
        id: str = None,
        sink: main_models.UpdatePipelineRequestSinkConditionRoutesSink = None,
    ):
        # The route expression in SPL. Only where, project, and extend are supported.
        self.expression = expression
        # The route ID.
        self.id = id
        # The write destination for the route.
        self.sink = sink

    def validate(self):
        if self.sink:
            self.sink.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.expression is not None:
            result['expression'] = self.expression

        if self.id is not None:
            result['id'] = self.id

        if self.sink is not None:
            result['sink'] = self.sink.to_map()

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('expression') is not None:
            self.expression = m.get('expression')

        if m.get('id') is not None:
            self.id = m.get('id')

        if m.get('sink') is not None:
            temp_model = main_models.UpdatePipelineRequestSinkConditionRoutesSink()
            self.sink = temp_model.from_map(m.get('sink'))

        return self

class UpdatePipelineRequestSinkConditionRoutesSink(DaraModel):
    def __init__(
        self,
        dataset: main_models.UpdatePipelineRequestSinkConditionRoutesSinkDataset = None,
        type: str = None,
    ):
        # The destination dataset for the route.
        self.dataset = dataset
        # The route destination type. Currently, only dataset is supported.
        self.type = type

    def validate(self):
        if self.dataset:
            self.dataset.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.dataset is not None:
            result['dataset'] = self.dataset.to_map()

        if self.type is not None:
            result['type'] = self.type

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('dataset') is not None:
            temp_model = main_models.UpdatePipelineRequestSinkConditionRoutesSinkDataset()
            self.dataset = temp_model.from_map(m.get('dataset'))

        if m.get('type') is not None:
            self.type = m.get('type')

        return self

class UpdatePipelineRequestSinkConditionRoutesSinkDataset(DaraModel):
    def __init__(
        self,
        agent_space: str = None,
        dataset: str = None,
    ):
        # The name of the AgentSpace to which the destination dataset belongs.
        self.agent_space = agent_space
        # The name of the destination dataset.
        self.dataset = dataset

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.agent_space is not None:
            result['agentSpace'] = self.agent_space

        if self.dataset is not None:
            result['dataset'] = self.dataset

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('agentSpace') is not None:
            self.agent_space = m.get('agentSpace')

        if m.get('dataset') is not None:
            self.dataset = m.get('dataset')

        return self

class UpdatePipelineRequestSinkConditionDefaultSink(DaraModel):
    def __init__(
        self,
        dataset: main_models.UpdatePipelineRequestSinkConditionDefaultSinkDataset = None,
        type: str = None,
    ):
        # The default destination dataset.
        self.dataset = dataset
        # The default destination type. Currently, only dataset is supported.
        self.type = type

    def validate(self):
        if self.dataset:
            self.dataset.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.dataset is not None:
            result['dataset'] = self.dataset.to_map()

        if self.type is not None:
            result['type'] = self.type

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('dataset') is not None:
            temp_model = main_models.UpdatePipelineRequestSinkConditionDefaultSinkDataset()
            self.dataset = temp_model.from_map(m.get('dataset'))

        if m.get('type') is not None:
            self.type = m.get('type')

        return self

class UpdatePipelineRequestSinkConditionDefaultSinkDataset(DaraModel):
    def __init__(
        self,
        agent_space: str = None,
        dataset: str = None,
    ):
        # The name of the AgentSpace to which the default destination dataset belongs.
        self.agent_space = agent_space
        # The name of the default destination dataset.
        self.dataset = dataset

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.agent_space is not None:
            result['agentSpace'] = self.agent_space

        if self.dataset is not None:
            result['dataset'] = self.dataset

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('agentSpace') is not None:
            self.agent_space = m.get('agentSpace')

        if m.get('dataset') is not None:
            self.dataset = m.get('dataset')

        return self

class UpdatePipelineRequestPipeline(DaraModel):
    def __init__(
        self,
        nodes: List[main_models.UpdatePipelineRequestPipelineNodes] = None,
    ):
        # The list of nodes.
        self.nodes = nodes

    def validate(self):
        if self.nodes:
            for v1 in self.nodes:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['nodes'] = []
        if self.nodes is not None:
            for k1 in self.nodes:
                result['nodes'].append(k1.to_map() if k1 else None)

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.nodes = []
        if m.get('nodes') is not None:
            for k1 in m.get('nodes'):
                temp_model = main_models.UpdatePipelineRequestPipelineNodes()
                self.nodes.append(temp_model.from_map(k1))

        return self

class UpdatePipelineRequestPipelineNodes(DaraModel):
    def __init__(
        self,
        id: str = None,
        parameters: Dict[str, Any] = None,
        type: str = None,
    ):
        # The node ID.
        self.id = id
        # The node parameters in key-value format. The parameters vary depending on the node type.
        self.parameters = parameters
        # The node type.
        self.type = type

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.id is not None:
            result['id'] = self.id

        if self.parameters is not None:
            result['parameters'] = self.parameters

        if self.type is not None:
            result['type'] = self.type

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('id') is not None:
            self.id = m.get('id')

        if m.get('parameters') is not None:
            self.parameters = m.get('parameters')

        if m.get('type') is not None:
            self.type = m.get('type')

        return self

class UpdatePipelineRequestExecutePolicy(DaraModel):
    def __init__(
        self,
        mode: str = None,
        run_once: main_models.UpdatePipelineRequestExecutePolicyRunOnce = None,
        scheduled: main_models.UpdatePipelineRequestExecutePolicyScheduled = None,
    ):
        # The scheduling mode. For example, Scheduled (timed scheduling) or RunOnce (one-time execution).
        self.mode = mode
        # The configuration for one-time execution.
        self.run_once = run_once
        # The timed scheduling configuration.
        self.scheduled = scheduled

    def validate(self):
        if self.run_once:
            self.run_once.validate()
        if self.scheduled:
            self.scheduled.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.mode is not None:
            result['mode'] = self.mode

        if self.run_once is not None:
            result['runOnce'] = self.run_once.to_map()

        if self.scheduled is not None:
            result['scheduled'] = self.scheduled.to_map()

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('mode') is not None:
            self.mode = m.get('mode')

        if m.get('runOnce') is not None:
            temp_model = main_models.UpdatePipelineRequestExecutePolicyRunOnce()
            self.run_once = temp_model.from_map(m.get('runOnce'))

        if m.get('scheduled') is not None:
            temp_model = main_models.UpdatePipelineRequestExecutePolicyScheduled()
            self.scheduled = temp_model.from_map(m.get('scheduled'))

        return self

class UpdatePipelineRequestExecutePolicyScheduled(DaraModel):
    def __init__(
        self,
        from_time: int = None,
        interval: str = None,
    ):
        # The scheduling start time, in UNIX millisecond timestamp.
        self.from_time = from_time
        # The scheduling interval. For example, 1h.
        self.interval = interval

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.from_time is not None:
            result['fromTime'] = self.from_time

        if self.interval is not None:
            result['interval'] = self.interval

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('fromTime') is not None:
            self.from_time = m.get('fromTime')

        if m.get('interval') is not None:
            self.interval = m.get('interval')

        return self

class UpdatePipelineRequestExecutePolicyRunOnce(DaraModel):
    def __init__(
        self,
        from_time: int = None,
        to_time: int = None,
    ):
        # The start time for data processing, in UNIX millisecond timestamp.
        self.from_time = from_time
        # The end time for data processing, in UNIX millisecond timestamp.
        self.to_time = to_time

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.from_time is not None:
            result['fromTime'] = self.from_time

        if self.to_time is not None:
            result['toTime'] = self.to_time

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('fromTime') is not None:
            self.from_time = m.get('fromTime')

        if m.get('toTime') is not None:
            self.to_time = m.get('toTime')

        return self

