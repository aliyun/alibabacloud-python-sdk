# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from darabonba.model import DaraModel

class ModifyInstanceNameRequest(DaraModel):
    def __init__(
        self,
        client_token: str = None,
        cluster_id: str = None,
        cluster_name: str = None,
        region_id: str = None,
        zone_id: str = None,
    ):
        # The client token that is used to ensure the idempotence of the request and prevent repeated request submissions. This parameter value is generated by the client. The value must be unique among different requests, can contain a maximum of 64 ASCII characters, and cannot contain non-ASCII characters.
        self.client_token = client_token
        # The instance ID.
        # 
        # This parameter is required.
        self.cluster_id = cluster_id
        # The new name of the instance. You can call the [DescribeInstance](https://help.aliyun.com/document_detail/144594.html) operation to query the current instance name.
        # 
        # This parameter is required.
        self.cluster_name = cluster_name
        # The region ID of the instance. You can call the [DescribeRegions](https://help.aliyun.com/document_detail/144489.html) operation to query the region ID.
        # 
        # This parameter is required.
        self.region_id = region_id
        # The zone ID. You can call the [DescribeRegions](https://help.aliyun.com/document_detail/144489.html) operation to query the zone ID.
        self.zone_id = zone_id

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.client_token is not None:
            result['ClientToken'] = self.client_token

        if self.cluster_id is not None:
            result['ClusterId'] = self.cluster_id

        if self.cluster_name is not None:
            result['ClusterName'] = self.cluster_name

        if self.region_id is not None:
            result['RegionId'] = self.region_id

        if self.zone_id is not None:
            result['ZoneId'] = self.zone_id

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('ClientToken') is not None:
            self.client_token = m.get('ClientToken')

        if m.get('ClusterId') is not None:
            self.cluster_id = m.get('ClusterId')

        if m.get('ClusterName') is not None:
            self.cluster_name = m.get('ClusterName')

        if m.get('RegionId') is not None:
            self.region_id = m.get('RegionId')

        if m.get('ZoneId') is not None:
            self.zone_id = m.get('ZoneId')

        return self

