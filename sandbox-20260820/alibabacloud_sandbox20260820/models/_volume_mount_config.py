# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List

from alibabacloud_sandbox20260820 import models as main_models
from darabonba.model import DaraModel

class VolumeMountConfig(DaraModel):
    def __init__(
        self,
        role: str = None,
        vpc_config: main_models.VolumeMountConfigVpcConfig = None,
    ):
        # The RAM role that grants Function Compute the required permissions. Scenarios include:
        # 
        # - Sending logs generated by the function to your Logstore.
        # - Generating temporary access tokens for the function to access other cloud resources during execution.
        self.role = role
        # The VPC configuration.
        self.vpc_config = vpc_config

    def validate(self):
        if self.vpc_config:
            self.vpc_config.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.role is not None:
            result['role'] = self.role

        if self.vpc_config is not None:
            result['vpcConfig'] = self.vpc_config.to_map()

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('role') is not None:
            self.role = m.get('role')

        if m.get('vpcConfig') is not None:
            temp_model = main_models.VolumeMountConfigVpcConfig()
            self.vpc_config = temp_model.from_map(m.get('vpcConfig'))

        return self

class VolumeMountConfigVpcConfig(DaraModel):
    def __init__(
        self,
        security_group_id: str = None,
        v_switch_ids: List[str] = None,
        vpc_id: str = None,
    ):
        # The security group ID.
        self.security_group_id = security_group_id
        # The vSwitch IDs.
        self.v_switch_ids = v_switch_ids
        # VPC ID。
        self.vpc_id = vpc_id

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.security_group_id is not None:
            result['securityGroupId'] = self.security_group_id

        if self.v_switch_ids is not None:
            result['vSwitchIds'] = self.v_switch_ids

        if self.vpc_id is not None:
            result['vpcId'] = self.vpc_id

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('securityGroupId') is not None:
            self.security_group_id = m.get('securityGroupId')

        if m.get('vSwitchIds') is not None:
            self.v_switch_ids = m.get('vSwitchIds')

        if m.get('vpcId') is not None:
            self.vpc_id = m.get('vpcId')

        return self

