# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List

from alibabacloud_ebs20210730 import models as main_models
from darabonba.model import DaraModel

class ListReportsResponseBody(DaraModel):
    def __init__(
        self,
        history_reports: List[main_models.ListReportsResponseBodyHistoryReports] = None,
        next_token: str = None,
        page_number: int = None,
        page_size: int = None,
        request_id: str = None,
        total_count: int = None,
    ):
        # Historical reports.
        self.history_reports = history_reports
        # Query token (Token), the value is the NextToken parameter value returned from the previous API call.
        self.next_token = next_token
        # Page number for paginated queries.
        self.page_number = page_number
        # Number of records per page for paginated queries.
        self.page_size = page_size
        # Request ID, an identifier generated by Alibaba Cloud for this request.
        self.request_id = request_id
        # Total count.
        self.total_count = total_count

    def validate(self):
        if self.history_reports:
            for v1 in self.history_reports:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['HistoryReports'] = []
        if self.history_reports is not None:
            for k1 in self.history_reports:
                result['HistoryReports'].append(k1.to_map() if k1 else None)

        if self.next_token is not None:
            result['NextToken'] = self.next_token

        if self.page_number is not None:
            result['PageNumber'] = self.page_number

        if self.page_size is not None:
            result['PageSize'] = self.page_size

        if self.request_id is not None:
            result['RequestId'] = self.request_id

        if self.total_count is not None:
            result['TotalCount'] = self.total_count

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.history_reports = []
        if m.get('HistoryReports') is not None:
            for k1 in m.get('HistoryReports'):
                temp_model = main_models.ListReportsResponseBodyHistoryReports()
                self.history_reports.append(temp_model.from_map(k1))

        if m.get('NextToken') is not None:
            self.next_token = m.get('NextToken')

        if m.get('PageNumber') is not None:
            self.page_number = m.get('PageNumber')

        if m.get('PageSize') is not None:
            self.page_size = m.get('PageSize')

        if m.get('RequestId') is not None:
            self.request_id = m.get('RequestId')

        if m.get('TotalCount') is not None:
            self.total_count = m.get('TotalCount')

        return self

class ListReportsResponseBodyHistoryReports(DaraModel):
    def __init__(
        self,
        app_name: str = None,
        report_id: str = None,
        report_name: str = None,
        report_time: str = None,
        subscribe_period: str = None,
    ):
        # Application name.
        self.app_name = app_name
        # Report ID.
        self.report_id = report_id
        # Report name.
        self.report_name = report_name
        # Report generation time.
        self.report_time = report_time
        # Report subscription period.
        self.subscribe_period = subscribe_period

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.app_name is not None:
            result['AppName'] = self.app_name

        if self.report_id is not None:
            result['ReportId'] = self.report_id

        if self.report_name is not None:
            result['ReportName'] = self.report_name

        if self.report_time is not None:
            result['ReportTime'] = self.report_time

        if self.subscribe_period is not None:
            result['SubscribePeriod'] = self.subscribe_period

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('AppName') is not None:
            self.app_name = m.get('AppName')

        if m.get('ReportId') is not None:
            self.report_id = m.get('ReportId')

        if m.get('ReportName') is not None:
            self.report_name = m.get('ReportName')

        if m.get('ReportTime') is not None:
            self.report_time = m.get('ReportTime')

        if m.get('SubscribePeriod') is not None:
            self.subscribe_period = m.get('SubscribePeriod')

        return self

