# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List, Dict, Any

from alibabacloud_apig20240327 import models as main_models
from darabonba.model import DaraModel

class ListAiModelCardsResponseBody(DaraModel):
    def __init__(
        self,
        code: str = None,
        data: main_models.ListAiModelCardsResponseBodyData = None,
        message: str = None,
        request_id: str = None,
    ):
        # The response status code. Ok is returned when the request is successful.
        self.code = code
        # The response data.
        self.data = data
        # The response message. success is returned when the request is successful.
        self.message = message
        # The request ID, which is generated by the system and used to locate this call.
        self.request_id = request_id

    def validate(self):
        if self.data:
            self.data.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.code is not None:
            result['code'] = self.code

        if self.data is not None:
            result['data'] = self.data.to_map()

        if self.message is not None:
            result['message'] = self.message

        if self.request_id is not None:
            result['requestId'] = self.request_id

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('code') is not None:
            self.code = m.get('code')

        if m.get('data') is not None:
            temp_model = main_models.ListAiModelCardsResponseBodyData()
            self.data = temp_model.from_map(m.get('data'))

        if m.get('message') is not None:
            self.message = m.get('message')

        if m.get('requestId') is not None:
            self.request_id = m.get('requestId')

        return self

class ListAiModelCardsResponseBodyData(DaraModel):
    def __init__(
        self,
        items: List[main_models.ListAiModelCardsResponseBodyDataItems] = None,
        page_number: int = None,
        page_size: int = None,
        total_size: int = None,
    ):
        # The list of model cards.
        self.items = items
        # The current page number.
        self.page_number = page_number
        # The number of entries per page for the current page.
        self.page_size = page_size
        # The total number of model cards that match the query conditions.
        self.total_size = total_size

    def validate(self):
        if self.items:
            for v1 in self.items:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['items'] = []
        if self.items is not None:
            for k1 in self.items:
                result['items'].append(k1.to_map() if k1 else None)

        if self.page_number is not None:
            result['pageNumber'] = self.page_number

        if self.page_size is not None:
            result['pageSize'] = self.page_size

        if self.total_size is not None:
            result['totalSize'] = self.total_size

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.items = []
        if m.get('items') is not None:
            for k1 in m.get('items'):
                temp_model = main_models.ListAiModelCardsResponseBodyDataItems()
                self.items.append(temp_model.from_map(k1))

        if m.get('pageNumber') is not None:
            self.page_number = m.get('pageNumber')

        if m.get('pageSize') is not None:
            self.page_size = m.get('pageSize')

        if m.get('totalSize') is not None:
            self.total_size = m.get('totalSize')

        return self

class ListAiModelCardsResponseBodyDataItems(DaraModel):
    def __init__(
        self,
        available_paths: List[main_models.ListAiModelCardsResponseBodyDataItemsAvailablePaths] = None,
        credit: main_models.ListAiModelCardsResponseBodyDataItemsCredit = None,
        features: Dict[str, Any] = None,
        gateway_id: str = None,
        meta: main_models.ListAiModelCardsResponseBodyDataItemsMeta = None,
        model_card_id: str = None,
        model_name: str = None,
        model_provider: str = None,
        source: str = None,
        source_url: str = None,
        update_time: str = None,
    ):
        # The list of invocation paths supported by the model. Each item contains both path and type.
        self.available_paths = available_paths
        # The credit billing information of the model. The unit is Credits per million tokens.
        self.credit = credit
        # The model capability switches. Keys are model capability names supported by the API gateway, and values are Boolean values.
        self.features = features
        # The ID of the AI gateway instance to which the model card belongs.
        self.gateway_id = gateway_id
        # The token limits and input/output modality information of the model.
        self.meta = meta
        # The model card ID. The ID is generated by the system and starts with mc-.
        self.model_card_id = model_card_id
        # The model name.
        self.model_name = model_name
        # The model provider identifier.
        self.model_provider = model_provider
        # The source of the model card. For instance-level model cards, user is returned.
        self.source = source
        # The URL of the model metadata.
        self.source_url = source_url
        # The last update time of the model card, in the format of yyyy-MM-dd HH:mm:ss.
        self.update_time = update_time

    def validate(self):
        if self.available_paths:
            for v1 in self.available_paths:
                 if v1:
                    v1.validate()
        if self.credit:
            self.credit.validate()
        if self.meta:
            self.meta.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['availablePaths'] = []
        if self.available_paths is not None:
            for k1 in self.available_paths:
                result['availablePaths'].append(k1.to_map() if k1 else None)

        if self.credit is not None:
            result['credit'] = self.credit.to_map()

        if self.features is not None:
            result['features'] = self.features

        if self.gateway_id is not None:
            result['gatewayId'] = self.gateway_id

        if self.meta is not None:
            result['meta'] = self.meta.to_map()

        if self.model_card_id is not None:
            result['modelCardId'] = self.model_card_id

        if self.model_name is not None:
            result['modelName'] = self.model_name

        if self.model_provider is not None:
            result['modelProvider'] = self.model_provider

        if self.source is not None:
            result['source'] = self.source

        if self.source_url is not None:
            result['sourceURL'] = self.source_url

        if self.update_time is not None:
            result['updateTime'] = self.update_time

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.available_paths = []
        if m.get('availablePaths') is not None:
            for k1 in m.get('availablePaths'):
                temp_model = main_models.ListAiModelCardsResponseBodyDataItemsAvailablePaths()
                self.available_paths.append(temp_model.from_map(k1))

        if m.get('credit') is not None:
            temp_model = main_models.ListAiModelCardsResponseBodyDataItemsCredit()
            self.credit = temp_model.from_map(m.get('credit'))

        if m.get('features') is not None:
            self.features = m.get('features')

        if m.get('gatewayId') is not None:
            self.gateway_id = m.get('gatewayId')

        if m.get('meta') is not None:
            temp_model = main_models.ListAiModelCardsResponseBodyDataItemsMeta()
            self.meta = temp_model.from_map(m.get('meta'))

        if m.get('modelCardId') is not None:
            self.model_card_id = m.get('modelCardId')

        if m.get('modelName') is not None:
            self.model_name = m.get('modelName')

        if m.get('modelProvider') is not None:
            self.model_provider = m.get('modelProvider')

        if m.get('source') is not None:
            self.source = m.get('source')

        if m.get('sourceURL') is not None:
            self.source_url = m.get('sourceURL')

        if m.get('updateTime') is not None:
            self.update_time = m.get('updateTime')

        return self

class ListAiModelCardsResponseBodyDataItemsMeta(DaraModel):
    def __init__(
        self,
        max_input_tokens: int = None,
        max_output_tokens: int = None,
        max_tokens: int = None,
        supported_input_modalities: List[str] = None,
        supported_output_modalities: List[str] = None,
    ):
        # The maximum number of input tokens supported by the model. The value must be greater than or equal to 0.
        self.max_input_tokens = max_input_tokens
        # The maximum number of output tokens supported by the model. The value must be greater than or equal to 0.
        self.max_output_tokens = max_output_tokens
        # The maximum total number of context tokens supported by the model. The value must be greater than or equal to 0.
        self.max_tokens = max_tokens
        # The list of input modalities supported by the model. The list contains up to 16 items, and each item must not be empty.
        self.supported_input_modalities = supported_input_modalities
        # The list of output modalities supported by the model. The list contains up to 16 items, and each item must not be empty.
        self.supported_output_modalities = supported_output_modalities

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.max_input_tokens is not None:
            result['maxInputTokens'] = self.max_input_tokens

        if self.max_output_tokens is not None:
            result['maxOutputTokens'] = self.max_output_tokens

        if self.max_tokens is not None:
            result['maxTokens'] = self.max_tokens

        if self.supported_input_modalities is not None:
            result['supportedInputModalities'] = self.supported_input_modalities

        if self.supported_output_modalities is not None:
            result['supportedOutputModalities'] = self.supported_output_modalities

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('maxInputTokens') is not None:
            self.max_input_tokens = m.get('maxInputTokens')

        if m.get('maxOutputTokens') is not None:
            self.max_output_tokens = m.get('maxOutputTokens')

        if m.get('maxTokens') is not None:
            self.max_tokens = m.get('maxTokens')

        if m.get('supportedInputModalities') is not None:
            self.supported_input_modalities = m.get('supportedInputModalities')

        if m.get('supportedOutputModalities') is not None:
            self.supported_output_modalities = m.get('supportedOutputModalities')

        return self

class ListAiModelCardsResponseBodyDataItemsCredit(DaraModel):
    def __init__(
        self,
        cache_cost: float = None,
        input_cost: float = None,
        output_cost: float = None,
        type: str = None,
    ):
        # The cost of cache-hit tokens, in Credits per million tokens.
        self.cache_cost = cache_cost
        # The cost of input tokens, in Credits per million tokens.
        self.input_cost = input_cost
        # The cost of output tokens, in Credits per million tokens.
        self.output_cost = output_cost
        # The billing type. Currently, only fixed is supported.
        self.type = type

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.cache_cost is not None:
            result['cacheCost'] = self.cache_cost

        if self.input_cost is not None:
            result['inputCost'] = self.input_cost

        if self.output_cost is not None:
            result['outputCost'] = self.output_cost

        if self.type is not None:
            result['type'] = self.type

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('cacheCost') is not None:
            self.cache_cost = m.get('cacheCost')

        if m.get('inputCost') is not None:
            self.input_cost = m.get('inputCost')

        if m.get('outputCost') is not None:
            self.output_cost = m.get('outputCost')

        if m.get('type') is not None:
            self.type = m.get('type')

        return self

class ListAiModelCardsResponseBodyDataItemsAvailablePaths(DaraModel):
    def __init__(
        self,
        path: str = None,
        type: str = None,
    ):
        # The model invocation path. The maximum length is 2048 characters.
        self.path = path
        # The protocol type corresponding to the path. The maximum length is 64 characters.
        self.type = type

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.path is not None:
            result['path'] = self.path

        if self.type is not None:
            result['type'] = self.type

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('path') is not None:
            self.path = m.get('path')

        if m.get('type') is not None:
            self.type = m.get('type')

        return self

