# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List

from alibabacloud_kms20160120 import models as main_models
from darabonba.model import DaraModel

class ListKeysResponseBody(DaraModel):
    def __init__(
        self,
        keys: main_models.ListKeysResponseBodyKeys = None,
        page_number: int = None,
        page_size: int = None,
        request_id: str = None,
        total_count: int = None,
    ):
        self.keys = keys
        # The page number.
        self.page_number = page_number
        # The number of entries returned per page.
        self.page_size = page_size
        # The ID of the request. It is a globally unique identifier (GUID) generated by Alibaba Cloud. You can use the request ID to troubleshoot issues.
        self.request_id = request_id
        # The total number of master keys.
        self.total_count = total_count

    def validate(self):
        if self.keys:
            self.keys.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.keys is not None:
            result['Keys'] = self.keys.to_map()

        if self.page_number is not None:
            result['PageNumber'] = self.page_number

        if self.page_size is not None:
            result['PageSize'] = self.page_size

        if self.request_id is not None:
            result['RequestId'] = self.request_id

        if self.total_count is not None:
            result['TotalCount'] = self.total_count

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Keys') is not None:
            temp_model = main_models.ListKeysResponseBodyKeys()
            self.keys = temp_model.from_map(m.get('Keys'))

        if m.get('PageNumber') is not None:
            self.page_number = m.get('PageNumber')

        if m.get('PageSize') is not None:
            self.page_size = m.get('PageSize')

        if m.get('RequestId') is not None:
            self.request_id = m.get('RequestId')

        if m.get('TotalCount') is not None:
            self.total_count = m.get('TotalCount')

        return self

class ListKeysResponseBodyKeys(DaraModel):
    def __init__(
        self,
        key: List[main_models.ListKeysResponseBodyKeysKey] = None,
    ):
        self.key = key

    def validate(self):
        if self.key:
            for v1 in self.key:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['Key'] = []
        if self.key is not None:
            for k1 in self.key:
                result['Key'].append(k1.to_map() if k1 else None)

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.key = []
        if m.get('Key') is not None:
            for k1 in m.get('Key'):
                temp_model = main_models.ListKeysResponseBodyKeysKey()
                self.key.append(temp_model.from_map(k1))

        return self

class ListKeysResponseBodyKeysKey(DaraModel):
    def __init__(
        self,
        key_arn: str = None,
        key_id: str = None,
    ):
        self.key_arn = key_arn
        self.key_id = key_id

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.key_arn is not None:
            result['KeyArn'] = self.key_arn

        if self.key_id is not None:
            result['KeyId'] = self.key_id

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('KeyArn') is not None:
            self.key_arn = m.get('KeyArn')

        if m.get('KeyId') is not None:
            self.key_id = m.get('KeyId')

        return self

