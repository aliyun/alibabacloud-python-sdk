# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List

from alibabacloud_kms20160120 import models as main_models
from darabonba.model import DaraModel

class ListManagedDataKeyVersionsResponseBody(DaraModel):
    def __init__(
        self,
        data_key_name: str = None,
        managed_data_key_versions: main_models.ListManagedDataKeyVersionsResponseBodyManagedDataKeyVersions = None,
        page_number: int = None,
        page_size: int = None,
        request_id: str = None,
        total_count: int = None,
    ):
        # The name of the managed data key (DK).
        self.data_key_name = data_key_name
        # The managed data key (DK) version information.
        self.managed_data_key_versions = managed_data_key_versions
        # The page number. The value must be an integer greater than 0. Default value: 1.
        self.page_number = page_number
        # The number of entries per page. Valid values: 1 to 100. Default value: 10.
        self.page_size = page_size
        # The request ID. It is a unique identifier generated by Alibaba Cloud for the request and can be used to troubleshoot issues.
        self.request_id = request_id
        # The total number of entries.
        self.total_count = total_count

    def validate(self):
        if self.managed_data_key_versions:
            self.managed_data_key_versions.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.data_key_name is not None:
            result['DataKeyName'] = self.data_key_name

        if self.managed_data_key_versions is not None:
            result['ManagedDataKeyVersions'] = self.managed_data_key_versions.to_map()

        if self.page_number is not None:
            result['PageNumber'] = self.page_number

        if self.page_size is not None:
            result['PageSize'] = self.page_size

        if self.request_id is not None:
            result['RequestId'] = self.request_id

        if self.total_count is not None:
            result['TotalCount'] = self.total_count

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('DataKeyName') is not None:
            self.data_key_name = m.get('DataKeyName')

        if m.get('ManagedDataKeyVersions') is not None:
            temp_model = main_models.ListManagedDataKeyVersionsResponseBodyManagedDataKeyVersions()
            self.managed_data_key_versions = temp_model.from_map(m.get('ManagedDataKeyVersions'))

        if m.get('PageNumber') is not None:
            self.page_number = m.get('PageNumber')

        if m.get('PageSize') is not None:
            self.page_size = m.get('PageSize')

        if m.get('RequestId') is not None:
            self.request_id = m.get('RequestId')

        if m.get('TotalCount') is not None:
            self.total_count = m.get('TotalCount')

        return self

class ListManagedDataKeyVersionsResponseBodyManagedDataKeyVersions(DaraModel):
    def __init__(
        self,
        managed_data_key_version: List[main_models.ListManagedDataKeyVersionsResponseBodyManagedDataKeyVersionsManagedDataKeyVersion] = None,
    ):
        # The list of managed data key (DK) versions.
        self.managed_data_key_version = managed_data_key_version

    def validate(self):
        if self.managed_data_key_version:
            for v1 in self.managed_data_key_version:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['ManagedDataKeyVersion'] = []
        if self.managed_data_key_version is not None:
            for k1 in self.managed_data_key_version:
                result['ManagedDataKeyVersion'].append(k1.to_map() if k1 else None)

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.managed_data_key_version = []
        if m.get('ManagedDataKeyVersion') is not None:
            for k1 in m.get('ManagedDataKeyVersion'):
                temp_model = main_models.ListManagedDataKeyVersionsResponseBodyManagedDataKeyVersionsManagedDataKeyVersion()
                self.managed_data_key_version.append(temp_model.from_map(k1))

        return self

class ListManagedDataKeyVersionsResponseBodyManagedDataKeyVersionsManagedDataKeyVersion(DaraModel):
    def __init__(
        self,
        data_key_version_id: str = None,
        data_key_version_name: str = None,
    ):
        # The version number of the managed data key (DK).
        self.data_key_version_id = data_key_version_id
        # The name of the credential that stores the key material of this managed data key (DK) version.
        self.data_key_version_name = data_key_version_name

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.data_key_version_id is not None:
            result['DataKeyVersionId'] = self.data_key_version_id

        if self.data_key_version_name is not None:
            result['DataKeyVersionName'] = self.data_key_version_name

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('DataKeyVersionId') is not None:
            self.data_key_version_id = m.get('DataKeyVersionId')

        if m.get('DataKeyVersionName') is not None:
            self.data_key_version_name = m.get('DataKeyVersionName')

        return self

