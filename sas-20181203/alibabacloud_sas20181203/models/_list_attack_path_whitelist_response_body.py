# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List

from alibabacloud_sas20181203 import models as main_models
from darabonba.model import DaraModel

class ListAttackPathWhitelistResponseBody(DaraModel):
    def __init__(
        self,
        attack_path_whitelist_list: List[main_models.ListAttackPathWhitelistResponseBodyAttackPathWhitelistList] = None,
        page_info: main_models.ListAttackPathWhitelistResponseBodyPageInfo = None,
        request_id: str = None,
    ):
        # List of attack path whitelists.
        self.attack_path_whitelist_list = attack_path_whitelist_list
        # Page information for paginated queries.
        self.page_info = page_info
        # The ID of this request, which is a unique identifier generated by Alibaba Cloud for the request and can be used for troubleshooting and problem localization.
        self.request_id = request_id

    def validate(self):
        if self.attack_path_whitelist_list:
            for v1 in self.attack_path_whitelist_list:
                 if v1:
                    v1.validate()
        if self.page_info:
            self.page_info.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['AttackPathWhitelistList'] = []
        if self.attack_path_whitelist_list is not None:
            for k1 in self.attack_path_whitelist_list:
                result['AttackPathWhitelistList'].append(k1.to_map() if k1 else None)

        if self.page_info is not None:
            result['PageInfo'] = self.page_info.to_map()

        if self.request_id is not None:
            result['RequestId'] = self.request_id

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.attack_path_whitelist_list = []
        if m.get('AttackPathWhitelistList') is not None:
            for k1 in m.get('AttackPathWhitelistList'):
                temp_model = main_models.ListAttackPathWhitelistResponseBodyAttackPathWhitelistList()
                self.attack_path_whitelist_list.append(temp_model.from_map(k1))

        if m.get('PageInfo') is not None:
            temp_model = main_models.ListAttackPathWhitelistResponseBodyPageInfo()
            self.page_info = temp_model.from_map(m.get('PageInfo'))

        if m.get('RequestId') is not None:
            self.request_id = m.get('RequestId')

        return self

class ListAttackPathWhitelistResponseBodyPageInfo(DaraModel):
    def __init__(
        self,
        count: int = None,
        current_page: int = None,
        page_size: int = None,
        total_count: int = None,
    ):
        # Number of items displayed on the current page in a paginated query.
        self.count = count
        # Current page number in a paginated query.
        self.current_page = current_page
        # Number of items per page in the returned data.
        self.page_size = page_size
        # Total number of items.
        self.total_count = total_count

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.count is not None:
            result['Count'] = self.count

        if self.current_page is not None:
            result['CurrentPage'] = self.current_page

        if self.page_size is not None:
            result['PageSize'] = self.page_size

        if self.total_count is not None:
            result['TotalCount'] = self.total_count

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Count') is not None:
            self.count = m.get('Count')

        if m.get('CurrentPage') is not None:
            self.current_page = m.get('CurrentPage')

        if m.get('PageSize') is not None:
            self.page_size = m.get('PageSize')

        if m.get('TotalCount') is not None:
            self.total_count = m.get('TotalCount')

        return self

class ListAttackPathWhitelistResponseBodyAttackPathWhitelistList(DaraModel):
    def __init__(
        self,
        attack_path_whitelist_id: str = None,
        last_modified_timestamp: int = None,
        path_name: str = None,
        path_type: str = None,
        remark: str = None,
        whitelist_name: str = None,
        whitelist_type: str = None,
    ):
        # Attack path whitelist ID.
        self.attack_path_whitelist_id = attack_path_whitelist_id
        # Timestamp of the last modification, in milliseconds.
        self.last_modified_timestamp = last_modified_timestamp
        # Path name.
        self.path_name = path_name
        # Path type.
        self.path_type = path_type
        # Remark.
        self.remark = remark
        # Whitelist name.
        self.whitelist_name = whitelist_name
        # Whitelist type.
        self.whitelist_type = whitelist_type

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.attack_path_whitelist_id is not None:
            result['AttackPathWhitelistId'] = self.attack_path_whitelist_id

        if self.last_modified_timestamp is not None:
            result['LastModifiedTimestamp'] = self.last_modified_timestamp

        if self.path_name is not None:
            result['PathName'] = self.path_name

        if self.path_type is not None:
            result['PathType'] = self.path_type

        if self.remark is not None:
            result['Remark'] = self.remark

        if self.whitelist_name is not None:
            result['WhitelistName'] = self.whitelist_name

        if self.whitelist_type is not None:
            result['WhitelistType'] = self.whitelist_type

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('AttackPathWhitelistId') is not None:
            self.attack_path_whitelist_id = m.get('AttackPathWhitelistId')

        if m.get('LastModifiedTimestamp') is not None:
            self.last_modified_timestamp = m.get('LastModifiedTimestamp')

        if m.get('PathName') is not None:
            self.path_name = m.get('PathName')

        if m.get('PathType') is not None:
            self.path_type = m.get('PathType')

        if m.get('Remark') is not None:
            self.remark = m.get('Remark')

        if m.get('WhitelistName') is not None:
            self.whitelist_name = m.get('WhitelistName')

        if m.get('WhitelistType') is not None:
            self.whitelist_type = m.get('WhitelistType')

        return self

