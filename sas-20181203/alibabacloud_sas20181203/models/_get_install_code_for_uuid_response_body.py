# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from alibabacloud_sas20181203 import models as main_models
from darabonba.model import DaraModel

class GetInstallCodeForUuidResponseBody(DaraModel):
    def __init__(
        self,
        code: int = None,
        http_status_code: int = None,
        install_code: main_models.GetInstallCodeForUuidResponseBodyInstallCode = None,
        message: str = None,
        request_id: str = None,
        success: bool = None,
    ):
        # API status code.
        self.code = code
        # HTTP status code
        self.http_status_code = http_status_code
        # Client installation code.
        self.install_code = install_code
        # Response message from the request.
        self.message = message
        # The ID of the current request, a unique identifier generated by Alibaba Cloud for troubleshooting and issue localization.
        self.request_id = request_id
        # The status of the returned query result. Values: - **true**: Success - **false**: Failure
        self.success = success

    def validate(self):
        if self.install_code:
            self.install_code.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.code is not None:
            result['Code'] = self.code

        if self.http_status_code is not None:
            result['HttpStatusCode'] = self.http_status_code

        if self.install_code is not None:
            result['InstallCode'] = self.install_code.to_map()

        if self.message is not None:
            result['Message'] = self.message

        if self.request_id is not None:
            result['RequestId'] = self.request_id

        if self.success is not None:
            result['Success'] = self.success

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Code') is not None:
            self.code = m.get('Code')

        if m.get('HttpStatusCode') is not None:
            self.http_status_code = m.get('HttpStatusCode')

        if m.get('InstallCode') is not None:
            temp_model = main_models.GetInstallCodeForUuidResponseBodyInstallCode()
            self.install_code = temp_model.from_map(m.get('InstallCode'))

        if m.get('Message') is not None:
            self.message = m.get('Message')

        if m.get('RequestId') is not None:
            self.request_id = m.get('RequestId')

        if m.get('Success') is not None:
            self.success = m.get('Success')

        return self

class GetInstallCodeForUuidResponseBodyInstallCode(DaraModel):
    def __init__(
        self,
        captcha_code: str = None,
        expired_date: int = None,
        group_id: int = None,
        group_name: str = None,
        os: str = None,
        vendor_name: str = None,
    ):
        # Installation verification code for manually installing the Cloud Security Center client.
        self.captcha_code = captcha_code
        # Expiration date of the installation verification code. The format is a 13-digit timestamp.
        # > The installation verification code can only be used within its validity period. Expired codes cannot be used.
        self.expired_date = expired_date
        # Server group ID.
        self.group_id = group_id
        # Server group name.
        self.group_name = group_name
        # Operating system of the server. Values:
        # 
        # - **linux**: Linux
        # 
        # - **windows**: Windows
        self.os = os
        # Name of the server vendor.
        self.vendor_name = vendor_name

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.captcha_code is not None:
            result['CaptchaCode'] = self.captcha_code

        if self.expired_date is not None:
            result['ExpiredDate'] = self.expired_date

        if self.group_id is not None:
            result['GroupId'] = self.group_id

        if self.group_name is not None:
            result['GroupName'] = self.group_name

        if self.os is not None:
            result['Os'] = self.os

        if self.vendor_name is not None:
            result['VendorName'] = self.vendor_name

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('CaptchaCode') is not None:
            self.captcha_code = m.get('CaptchaCode')

        if m.get('ExpiredDate') is not None:
            self.expired_date = m.get('ExpiredDate')

        if m.get('GroupId') is not None:
            self.group_id = m.get('GroupId')

        if m.get('GroupName') is not None:
            self.group_name = m.get('GroupName')

        if m.get('Os') is not None:
            self.os = m.get('Os')

        if m.get('VendorName') is not None:
            self.vendor_name = m.get('VendorName')

        return self

