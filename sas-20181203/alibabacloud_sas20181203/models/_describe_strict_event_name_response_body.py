# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List

from alibabacloud_sas20181203 import models as main_models
from darabonba.model import DaraModel

class DescribeStrictEventNameResponseBody(DaraModel):
    def __init__(
        self,
        data: List[main_models.DescribeStrictEventNameResponseBodyData] = None,
        request_id: str = None,
    ):
        # Returned data.
        self.data = data
        # The ID of this call request, a unique identifier generated by Alibaba Cloud for this request, which can be used to troubleshoot and locate issues.
        self.request_id = request_id

    def validate(self):
        if self.data:
            for v1 in self.data:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['Data'] = []
        if self.data is not None:
            for k1 in self.data:
                result['Data'].append(k1.to_map() if k1 else None)

        if self.request_id is not None:
            result['RequestId'] = self.request_id

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.data = []
        if m.get('Data') is not None:
            for k1 in m.get('Data'):
                temp_model = main_models.DescribeStrictEventNameResponseBodyData()
                self.data.append(temp_model.from_map(k1))

        if m.get('RequestId') is not None:
            self.request_id = m.get('RequestId')

        return self

class DescribeStrictEventNameResponseBodyData(DaraModel):
    def __init__(
        self,
        event_name: str = None,
        event_type: str = None,
        show_event_name: str = None,
        show_event_type: str = None,
        switch_config: bool = None,
    ):
        # Alarm name.
        self.event_name = event_name
        # Alarm type.
        self.event_type = event_type
        # Displayed alarm name.
        self.show_event_name = show_event_name
        # Displayed alarm type.
        self.show_event_type = show_event_type
        # Indicates whether the user has enabled the strict mode for this alarm type.
        # - true: Enabled
        # - false: Not enabled
        self.switch_config = switch_config

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.event_name is not None:
            result['EventName'] = self.event_name

        if self.event_type is not None:
            result['EventType'] = self.event_type

        if self.show_event_name is not None:
            result['ShowEventName'] = self.show_event_name

        if self.show_event_type is not None:
            result['ShowEventType'] = self.show_event_type

        if self.switch_config is not None:
            result['SwitchConfig'] = self.switch_config

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('EventName') is not None:
            self.event_name = m.get('EventName')

        if m.get('EventType') is not None:
            self.event_type = m.get('EventType')

        if m.get('ShowEventName') is not None:
            self.show_event_name = m.get('ShowEventName')

        if m.get('ShowEventType') is not None:
            self.show_event_type = m.get('ShowEventType')

        if m.get('SwitchConfig') is not None:
            self.switch_config = m.get('SwitchConfig')

        return self

