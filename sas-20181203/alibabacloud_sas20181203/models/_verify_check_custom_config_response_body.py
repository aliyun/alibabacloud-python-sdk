# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List

from alibabacloud_sas20181203 import models as main_models
from darabonba.model import DaraModel

class VerifyCheckCustomConfigResponseBody(DaraModel):
    def __init__(
        self,
        error_check_configs: List[main_models.VerifyCheckCustomConfigResponseBodyErrorCheckConfigs] = None,
        error_check_custom_config: main_models.VerifyCheckCustomConfigResponseBodyErrorCheckCustomConfig = None,
        error_repair_configs: List[main_models.VerifyCheckCustomConfigResponseBodyErrorRepairConfigs] = None,
        request_id: str = None,
    ):
        # The list of check parameters that have configuration check errors.
        self.error_check_configs = error_check_configs
        # The error code returned for the custom check item validation.
        self.error_check_custom_config = error_check_custom_config
        # The list of repair parameters that have configuration check errors.
        self.error_repair_configs = error_repair_configs
        # The request ID, which is a unique identifier generated by Alibaba Cloud for the request. You can use this ID to troubleshoot issues.
        self.request_id = request_id

    def validate(self):
        if self.error_check_configs:
            for v1 in self.error_check_configs:
                 if v1:
                    v1.validate()
        if self.error_check_custom_config:
            self.error_check_custom_config.validate()
        if self.error_repair_configs:
            for v1 in self.error_repair_configs:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['ErrorCheckConfigs'] = []
        if self.error_check_configs is not None:
            for k1 in self.error_check_configs:
                result['ErrorCheckConfigs'].append(k1.to_map() if k1 else None)

        if self.error_check_custom_config is not None:
            result['ErrorCheckCustomConfig'] = self.error_check_custom_config.to_map()

        result['ErrorRepairConfigs'] = []
        if self.error_repair_configs is not None:
            for k1 in self.error_repair_configs:
                result['ErrorRepairConfigs'].append(k1.to_map() if k1 else None)

        if self.request_id is not None:
            result['RequestId'] = self.request_id

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.error_check_configs = []
        if m.get('ErrorCheckConfigs') is not None:
            for k1 in m.get('ErrorCheckConfigs'):
                temp_model = main_models.VerifyCheckCustomConfigResponseBodyErrorCheckConfigs()
                self.error_check_configs.append(temp_model.from_map(k1))

        if m.get('ErrorCheckCustomConfig') is not None:
            temp_model = main_models.VerifyCheckCustomConfigResponseBodyErrorCheckCustomConfig()
            self.error_check_custom_config = temp_model.from_map(m.get('ErrorCheckCustomConfig'))

        self.error_repair_configs = []
        if m.get('ErrorRepairConfigs') is not None:
            for k1 in m.get('ErrorRepairConfigs'):
                temp_model = main_models.VerifyCheckCustomConfigResponseBodyErrorRepairConfigs()
                self.error_repair_configs.append(temp_model.from_map(k1))

        if m.get('RequestId') is not None:
            self.request_id = m.get('RequestId')

        return self

class VerifyCheckCustomConfigResponseBodyErrorRepairConfigs(DaraModel):
    def __init__(
        self,
        error_code: str = None,
        error_msg: str = None,
        link_error_configs: List[main_models.VerifyCheckCustomConfigResponseBodyErrorRepairConfigsLinkErrorConfigs] = None,
        name: str = None,
        type: str = None,
        value: str = None,
    ):
        # The error code.
        self.error_code = error_code
        # The error message.
        self.error_msg = error_msg
        # The related configurations that caused this error.
        self.link_error_configs = link_error_configs
        # The name of the repair configuration item for the check item. The name is unique within the check item.
        self.name = name
        # The type of the parameter that has an error. Valid values:
        # - repair: repair parameter.
        self.type = type
        # The name of the repair parameter for the check item. The name is unique within the check item.
        self.value = value

    def validate(self):
        if self.link_error_configs:
            for v1 in self.link_error_configs:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.error_code is not None:
            result['ErrorCode'] = self.error_code

        if self.error_msg is not None:
            result['ErrorMsg'] = self.error_msg

        result['LinkErrorConfigs'] = []
        if self.link_error_configs is not None:
            for k1 in self.link_error_configs:
                result['LinkErrorConfigs'].append(k1.to_map() if k1 else None)

        if self.name is not None:
            result['Name'] = self.name

        if self.type is not None:
            result['Type'] = self.type

        if self.value is not None:
            result['Value'] = self.value

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('ErrorCode') is not None:
            self.error_code = m.get('ErrorCode')

        if m.get('ErrorMsg') is not None:
            self.error_msg = m.get('ErrorMsg')

        self.link_error_configs = []
        if m.get('LinkErrorConfigs') is not None:
            for k1 in m.get('LinkErrorConfigs'):
                temp_model = main_models.VerifyCheckCustomConfigResponseBodyErrorRepairConfigsLinkErrorConfigs()
                self.link_error_configs.append(temp_model.from_map(k1))

        if m.get('Name') is not None:
            self.name = m.get('Name')

        if m.get('Type') is not None:
            self.type = m.get('Type')

        if m.get('Value') is not None:
            self.value = m.get('Value')

        return self

class VerifyCheckCustomConfigResponseBodyErrorRepairConfigsLinkErrorConfigs(DaraModel):
    def __init__(
        self,
        name: str = None,
        show_name: str = None,
        type: str = None,
    ):
        # The name of the check item configuration. The name is unique within the check item.
        self.name = name
        # The display name of the user check configuration.
        self.show_name = show_name
        # The type of the parameter that has an error. Valid values:
        # - repair: repair parameter
        # - custom: custom configuration parameter.
        self.type = type

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.name is not None:
            result['Name'] = self.name

        if self.show_name is not None:
            result['ShowName'] = self.show_name

        if self.type is not None:
            result['Type'] = self.type

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Name') is not None:
            self.name = m.get('Name')

        if m.get('ShowName') is not None:
            self.show_name = m.get('ShowName')

        if m.get('Type') is not None:
            self.type = m.get('Type')

        return self

class VerifyCheckCustomConfigResponseBodyErrorCheckCustomConfig(DaraModel):
    def __init__(
        self,
        error_code: str = None,
        error_msg: str = None,
    ):
        # The error code returned upon failure. This parameter is not returned upon success. Valid values:
        # - **CspmVerifyCheckCustomItemNoPass**: The asset did not pass the custom rule that was configured.
        # - **CspmVerifyCheckCustomItemError**: A validation error occurred because the input rule has issues.
        self.error_code = error_code
        # The exception information.
        self.error_msg = error_msg

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.error_code is not None:
            result['ErrorCode'] = self.error_code

        if self.error_msg is not None:
            result['ErrorMsg'] = self.error_msg

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('ErrorCode') is not None:
            self.error_code = m.get('ErrorCode')

        if m.get('ErrorMsg') is not None:
            self.error_msg = m.get('ErrorMsg')

        return self

class VerifyCheckCustomConfigResponseBodyErrorCheckConfigs(DaraModel):
    def __init__(
        self,
        error_code: str = None,
        error_msg: str = None,
        link_error_configs: List[main_models.VerifyCheckCustomConfigResponseBodyErrorCheckConfigsLinkErrorConfigs] = None,
        name: str = None,
        type: str = None,
        value: str = None,
    ):
        # The error code.
        self.error_code = error_code
        # The error message.
        self.error_msg = error_msg
        # The related configurations that caused this error.
        self.link_error_configs = link_error_configs
        # The name of the custom configuration item for the check item. The name is unique within the check item.
        self.name = name
        # The type of the parameter that has an error. Valid values:
        # - custom: custom configuration parameter.
        self.type = type
        # The user-configured value string of the custom configuration item for the check item.
        self.value = value

    def validate(self):
        if self.link_error_configs:
            for v1 in self.link_error_configs:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.error_code is not None:
            result['ErrorCode'] = self.error_code

        if self.error_msg is not None:
            result['ErrorMsg'] = self.error_msg

        result['LinkErrorConfigs'] = []
        if self.link_error_configs is not None:
            for k1 in self.link_error_configs:
                result['LinkErrorConfigs'].append(k1.to_map() if k1 else None)

        if self.name is not None:
            result['Name'] = self.name

        if self.type is not None:
            result['Type'] = self.type

        if self.value is not None:
            result['Value'] = self.value

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('ErrorCode') is not None:
            self.error_code = m.get('ErrorCode')

        if m.get('ErrorMsg') is not None:
            self.error_msg = m.get('ErrorMsg')

        self.link_error_configs = []
        if m.get('LinkErrorConfigs') is not None:
            for k1 in m.get('LinkErrorConfigs'):
                temp_model = main_models.VerifyCheckCustomConfigResponseBodyErrorCheckConfigsLinkErrorConfigs()
                self.link_error_configs.append(temp_model.from_map(k1))

        if m.get('Name') is not None:
            self.name = m.get('Name')

        if m.get('Type') is not None:
            self.type = m.get('Type')

        if m.get('Value') is not None:
            self.value = m.get('Value')

        return self

class VerifyCheckCustomConfigResponseBodyErrorCheckConfigsLinkErrorConfigs(DaraModel):
    def __init__(
        self,
        name: str = None,
        show_name: str = None,
        type: str = None,
    ):
        # The name of the check item configuration. The name is unique within the check item.
        self.name = name
        # The display name of the user check configuration.
        self.show_name = show_name
        # The type of the parameter that has an error. Valid values:
        # - repair: repair parameter
        # - custom: custom configuration parameter.
        self.type = type

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.name is not None:
            result['Name'] = self.name

        if self.show_name is not None:
            result['ShowName'] = self.show_name

        if self.type is not None:
            result['Type'] = self.type

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Name') is not None:
            self.name = m.get('Name')

        if m.get('ShowName') is not None:
            self.show_name = m.get('ShowName')

        if m.get('Type') is not None:
            self.type = m.get('Type')

        return self

