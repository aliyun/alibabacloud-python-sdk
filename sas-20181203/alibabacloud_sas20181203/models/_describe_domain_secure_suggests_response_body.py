# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from darabonba.model import DaraModel

class DescribeDomainSecureSuggestsResponseBody(DaraModel):
    def __init__(
        self,
        request_id: str = None,
        risk_suggest: str = None,
        total_count: int = None,
        vul_suggest: int = None,
        web_lock_suggest: str = None,
    ):
        # The ID of this call request, a unique identifier generated by Alibaba Cloud for this request, which can be used to troubleshoot and locate issues.
        self.request_id = request_id
        # Overall suggestions for security reinforcement.
        self.risk_suggest = risk_suggest
        # Total number of security recommendations.
        self.total_count = total_count
        # Suggestions for vulnerability repair.
        self.vul_suggest = vul_suggest
        # Suggestions for website tamper prevention.
        self.web_lock_suggest = web_lock_suggest

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.request_id is not None:
            result['RequestId'] = self.request_id

        if self.risk_suggest is not None:
            result['RiskSuggest'] = self.risk_suggest

        if self.total_count is not None:
            result['TotalCount'] = self.total_count

        if self.vul_suggest is not None:
            result['VulSuggest'] = self.vul_suggest

        if self.web_lock_suggest is not None:
            result['WebLockSuggest'] = self.web_lock_suggest

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('RequestId') is not None:
            self.request_id = m.get('RequestId')

        if m.get('RiskSuggest') is not None:
            self.risk_suggest = m.get('RiskSuggest')

        if m.get('TotalCount') is not None:
            self.total_count = m.get('TotalCount')

        if m.get('VulSuggest') is not None:
            self.vul_suggest = m.get('VulSuggest')

        if m.get('WebLockSuggest') is not None:
            self.web_lock_suggest = m.get('WebLockSuggest')

        return self

