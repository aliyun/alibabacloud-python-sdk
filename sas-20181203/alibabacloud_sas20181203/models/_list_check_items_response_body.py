# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List

from alibabacloud_sas20181203 import models as main_models
from darabonba.model import DaraModel

class ListCheckItemsResponseBody(DaraModel):
    def __init__(
        self,
        check_items: List[main_models.ListCheckItemsResponseBodyCheckItems] = None,
        page_info: main_models.ListCheckItemsResponseBodyPageInfo = None,
        request_id: str = None,
    ):
        # Information about the check items.
        self.check_items = check_items
        # Page information for paginated queries.
        self.page_info = page_info
        # The ID of the current request, which is a unique identifier generated by Alibaba Cloud for this request and can be used for troubleshooting and problem localization.
        self.request_id = request_id

    def validate(self):
        if self.check_items:
            for v1 in self.check_items:
                 if v1:
                    v1.validate()
        if self.page_info:
            self.page_info.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['CheckItems'] = []
        if self.check_items is not None:
            for k1 in self.check_items:
                result['CheckItems'].append(k1.to_map() if k1 else None)

        if self.page_info is not None:
            result['PageInfo'] = self.page_info.to_map()

        if self.request_id is not None:
            result['RequestId'] = self.request_id

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.check_items = []
        if m.get('CheckItems') is not None:
            for k1 in m.get('CheckItems'):
                temp_model = main_models.ListCheckItemsResponseBodyCheckItems()
                self.check_items.append(temp_model.from_map(k1))

        if m.get('PageInfo') is not None:
            temp_model = main_models.ListCheckItemsResponseBodyPageInfo()
            self.page_info = temp_model.from_map(m.get('PageInfo'))

        if m.get('RequestId') is not None:
            self.request_id = m.get('RequestId')

        return self

class ListCheckItemsResponseBodyPageInfo(DaraModel):
    def __init__(
        self,
        count: int = None,
        current_page: int = None,
        page_size: int = None,
        total_count: int = None,
    ):
        # Number of data entries displayed on the current page in a paginated query.
        self.count = count
        # The page number of the current page when performing a paginated query.
        self.current_page = current_page
        # The maximum number of records displayed per page when performing a paginated query.
        self.page_size = page_size
        # Total number of data entries found.
        self.total_count = total_count

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.count is not None:
            result['Count'] = self.count

        if self.current_page is not None:
            result['CurrentPage'] = self.current_page

        if self.page_size is not None:
            result['PageSize'] = self.page_size

        if self.total_count is not None:
            result['TotalCount'] = self.total_count

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Count') is not None:
            self.count = m.get('Count')

        if m.get('CurrentPage') is not None:
            self.current_page = m.get('CurrentPage')

        if m.get('PageSize') is not None:
            self.page_size = m.get('PageSize')

        if m.get('TotalCount') is not None:
            self.total_count = m.get('TotalCount')

        return self

class ListCheckItemsResponseBodyCheckItems(DaraModel):
    def __init__(
        self,
        assist_info: main_models.ListCheckItemsResponseBodyCheckItemsAssistInfo = None,
        check_id: int = None,
        check_policies: List[main_models.ListCheckItemsResponseBodyCheckItemsCheckPolicies] = None,
        check_rule: str = None,
        check_show_name: str = None,
        check_type: str = None,
        description: main_models.ListCheckItemsResponseBodyCheckItemsDescription = None,
        instance_sub_type: str = None,
        instance_type: str = None,
        remark: str = None,
        risk_level: str = None,
        solution: main_models.ListCheckItemsResponseBodyCheckItemsSolution = None,
        status: str = None,
        vendor: str = None,
    ):
        # The help information for the check item.
        self.assist_info = assist_info
        # The ID of the check item.
        self.check_id = check_id
        # A list of standard, regulation, and section information associated with the check result.
        self.check_policies = check_policies
        # 自定义检查项定义规则。
        self.check_rule = check_rule
        # The name of the check item.
        self.check_show_name = check_show_name
        # The source type of the Security Posture check item:
        #  - **CUSTOM**: User-defined
        #  - **SYSTEM**: Predefined by the Security Posture platform
        self.check_type = check_type
        # The description information of the check item.
        self.description = description
        # The sub-type of the cloud product\\"s asset.
        self.instance_sub_type = instance_sub_type
        # The asset type of the cloud product.
        self.instance_type = instance_type
        # Remark information.
        self.remark = remark
        # The risk level of the check item. Values:
        # - **HIGH**: High risk
        # - **MEDIUM**: Medium risk
        # - **LOW**: Low risk
        self.risk_level = risk_level
        # The solution information for the check item.
        self.solution = solution
        # The status of the check item. Values:
        # - **EDIT**: In editing
        # - **RELEASE**: Released
        self.status = status
        # The cloud asset vendor.
        self.vendor = vendor

    def validate(self):
        if self.assist_info:
            self.assist_info.validate()
        if self.check_policies:
            for v1 in self.check_policies:
                 if v1:
                    v1.validate()
        if self.description:
            self.description.validate()
        if self.solution:
            self.solution.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.assist_info is not None:
            result['AssistInfo'] = self.assist_info.to_map()

        if self.check_id is not None:
            result['CheckId'] = self.check_id

        result['CheckPolicies'] = []
        if self.check_policies is not None:
            for k1 in self.check_policies:
                result['CheckPolicies'].append(k1.to_map() if k1 else None)

        if self.check_rule is not None:
            result['CheckRule'] = self.check_rule

        if self.check_show_name is not None:
            result['CheckShowName'] = self.check_show_name

        if self.check_type is not None:
            result['CheckType'] = self.check_type

        if self.description is not None:
            result['Description'] = self.description.to_map()

        if self.instance_sub_type is not None:
            result['InstanceSubType'] = self.instance_sub_type

        if self.instance_type is not None:
            result['InstanceType'] = self.instance_type

        if self.remark is not None:
            result['Remark'] = self.remark

        if self.risk_level is not None:
            result['RiskLevel'] = self.risk_level

        if self.solution is not None:
            result['Solution'] = self.solution.to_map()

        if self.status is not None:
            result['Status'] = self.status

        if self.vendor is not None:
            result['Vendor'] = self.vendor

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('AssistInfo') is not None:
            temp_model = main_models.ListCheckItemsResponseBodyCheckItemsAssistInfo()
            self.assist_info = temp_model.from_map(m.get('AssistInfo'))

        if m.get('CheckId') is not None:
            self.check_id = m.get('CheckId')

        self.check_policies = []
        if m.get('CheckPolicies') is not None:
            for k1 in m.get('CheckPolicies'):
                temp_model = main_models.ListCheckItemsResponseBodyCheckItemsCheckPolicies()
                self.check_policies.append(temp_model.from_map(k1))

        if m.get('CheckRule') is not None:
            self.check_rule = m.get('CheckRule')

        if m.get('CheckShowName') is not None:
            self.check_show_name = m.get('CheckShowName')

        if m.get('CheckType') is not None:
            self.check_type = m.get('CheckType')

        if m.get('Description') is not None:
            temp_model = main_models.ListCheckItemsResponseBodyCheckItemsDescription()
            self.description = temp_model.from_map(m.get('Description'))

        if m.get('InstanceSubType') is not None:
            self.instance_sub_type = m.get('InstanceSubType')

        if m.get('InstanceType') is not None:
            self.instance_type = m.get('InstanceType')

        if m.get('Remark') is not None:
            self.remark = m.get('Remark')

        if m.get('RiskLevel') is not None:
            self.risk_level = m.get('RiskLevel')

        if m.get('Solution') is not None:
            temp_model = main_models.ListCheckItemsResponseBodyCheckItemsSolution()
            self.solution = temp_model.from_map(m.get('Solution'))

        if m.get('Status') is not None:
            self.status = m.get('Status')

        if m.get('Vendor') is not None:
            self.vendor = m.get('Vendor')

        return self

class ListCheckItemsResponseBodyCheckItemsSolution(DaraModel):
    def __init__(
        self,
        type: str = None,
        value: str = None,
    ):
        # The type of the check item solution information. Values:
        # 
        # - **text**：Text
        self.type = type
        # The content of the solution for the check item risk.
        self.value = value

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.type is not None:
            result['Type'] = self.type

        if self.value is not None:
            result['Value'] = self.value

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Type') is not None:
            self.type = m.get('Type')

        if m.get('Value') is not None:
            self.value = m.get('Value')

        return self

class ListCheckItemsResponseBodyCheckItemsDescription(DaraModel):
    def __init__(
        self,
        type: str = None,
        value: str = None,
    ):
        # The type of the check description attribute. Values:
        # 
        # - **text**：Text
        self.type = type
        # The specific content of the description.
        self.value = value

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.type is not None:
            result['Type'] = self.type

        if self.value is not None:
            result['Value'] = self.value

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Type') is not None:
            self.type = m.get('Type')

        if m.get('Value') is not None:
            self.value = m.get('Value')

        return self

class ListCheckItemsResponseBodyCheckItemsCheckPolicies(DaraModel):
    def __init__(
        self,
        requirement_id: int = None,
        requirement_show_name: str = None,
        section_id: int = None,
        section_show_name: str = None,
        standard_id: int = None,
        standard_show_name: str = None,
        type: str = None,
    ):
        # The ID of the requirement for the check item.
        self.requirement_id = requirement_id
        # The display name of the requirement for the check item.
        self.requirement_show_name = requirement_show_name
        # The ID of the section for the check item.
        self.section_id = section_id
        # The display name of the section for the check item.
        self.section_show_name = section_show_name
        # 检查项的标准ID。
        self.standard_id = standard_id
        # The display name of the standard for the check item.
        self.standard_show_name = standard_show_name
        # The name of the associated policy category:
        # - **AISPM**：AI Configuration Management (AI-SPM)
        # - **KISPM**：Kubernetes Configuration Management (KSPM)
        # - **IDENTITY_PERMISSION**：Identity and Permission Management (CIEM)
        # - **RISK**：Security Risk
        # - **COMPLIANCE**：Compliance Risk
        self.type = type

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.requirement_id is not None:
            result['RequirementId'] = self.requirement_id

        if self.requirement_show_name is not None:
            result['RequirementShowName'] = self.requirement_show_name

        if self.section_id is not None:
            result['SectionId'] = self.section_id

        if self.section_show_name is not None:
            result['SectionShowName'] = self.section_show_name

        if self.standard_id is not None:
            result['StandardId'] = self.standard_id

        if self.standard_show_name is not None:
            result['StandardShowName'] = self.standard_show_name

        if self.type is not None:
            result['Type'] = self.type

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('RequirementId') is not None:
            self.requirement_id = m.get('RequirementId')

        if m.get('RequirementShowName') is not None:
            self.requirement_show_name = m.get('RequirementShowName')

        if m.get('SectionId') is not None:
            self.section_id = m.get('SectionId')

        if m.get('SectionShowName') is not None:
            self.section_show_name = m.get('SectionShowName')

        if m.get('StandardId') is not None:
            self.standard_id = m.get('StandardId')

        if m.get('StandardShowName') is not None:
            self.standard_show_name = m.get('StandardShowName')

        if m.get('Type') is not None:
            self.type = m.get('Type')

        return self

class ListCheckItemsResponseBodyCheckItemsAssistInfo(DaraModel):
    def __init__(
        self,
        type: str = None,
        value: str = None,
    ):
        # The type of the help information for the check item risk. Values:
        # 
        # - **text**：Text
        self.type = type
        # The content of the help information for the check item risk.
        self.value = value

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.type is not None:
            result['Type'] = self.type

        if self.value is not None:
            result['Value'] = self.value

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Type') is not None:
            self.type = m.get('Type')

        if m.get('Value') is not None:
            self.value = m.get('Value')

        return self

