# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List

from alibabacloud_sas20181203 import models as main_models
from darabonba.model import DaraModel

class ListAttackPathEventResponseBody(DaraModel):
    def __init__(
        self,
        attack_path_event_list: List[main_models.ListAttackPathEventResponseBodyAttackPathEventList] = None,
        page_info: main_models.ListAttackPathEventResponseBodyPageInfo = None,
        request_id: str = None,
    ):
        # List of attack path events.
        self.attack_path_event_list = attack_path_event_list
        # Pagination information.
        self.page_info = page_info
        # The ID of this request, which is a unique identifier generated by Alibaba Cloud for the request. It can be used for troubleshooting and problem localization.
        self.request_id = request_id

    def validate(self):
        if self.attack_path_event_list:
            for v1 in self.attack_path_event_list:
                 if v1:
                    v1.validate()
        if self.page_info:
            self.page_info.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['AttackPathEventList'] = []
        if self.attack_path_event_list is not None:
            for k1 in self.attack_path_event_list:
                result['AttackPathEventList'].append(k1.to_map() if k1 else None)

        if self.page_info is not None:
            result['PageInfo'] = self.page_info.to_map()

        if self.request_id is not None:
            result['RequestId'] = self.request_id

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.attack_path_event_list = []
        if m.get('AttackPathEventList') is not None:
            for k1 in m.get('AttackPathEventList'):
                temp_model = main_models.ListAttackPathEventResponseBodyAttackPathEventList()
                self.attack_path_event_list.append(temp_model.from_map(k1))

        if m.get('PageInfo') is not None:
            temp_model = main_models.ListAttackPathEventResponseBodyPageInfo()
            self.page_info = temp_model.from_map(m.get('PageInfo'))

        if m.get('RequestId') is not None:
            self.request_id = m.get('RequestId')

        return self

class ListAttackPathEventResponseBodyPageInfo(DaraModel):
    def __init__(
        self,
        count: int = None,
        current_page: int = None,
        page_size: int = None,
        total_count: int = None,
    ):
        # The number of data entries displayed on the current page when performing a paginated query.
        self.count = count
        # The page number displayed in the pagination query.
        self.current_page = current_page
        # The number of data items displayed per page.
        self.page_size = page_size
        # The total number of data items.
        self.total_count = total_count

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.count is not None:
            result['Count'] = self.count

        if self.current_page is not None:
            result['CurrentPage'] = self.current_page

        if self.page_size is not None:
            result['PageSize'] = self.page_size

        if self.total_count is not None:
            result['TotalCount'] = self.total_count

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Count') is not None:
            self.count = m.get('Count')

        if m.get('CurrentPage') is not None:
            self.current_page = m.get('CurrentPage')

        if m.get('PageSize') is not None:
            self.page_size = m.get('PageSize')

        if m.get('TotalCount') is not None:
            self.total_count = m.get('TotalCount')

        return self

class ListAttackPathEventResponseBodyAttackPathEventList(DaraModel):
    def __init__(
        self,
        dst_asset: main_models.ListAttackPathEventResponseBodyAttackPathEventListDstAsset = None,
        event_id: int = None,
        last_time: int = None,
        path_name: str = None,
        path_name_desc: str = None,
        path_type: str = None,
        path_type_desc: str = None,
        risk_level: str = None,
        src_asset: main_models.ListAttackPathEventResponseBodyAttackPathEventListSrcAsset = None,
    ):
        # Destination asset.
        self.dst_asset = dst_asset
        # The ID of the alert event.
        self.event_id = event_id
        # Timestamp of the latest discovery. Unit: milliseconds.
        self.last_time = last_time
        # Path name.
        self.path_name = path_name
        # Description of the path name.
        self.path_name_desc = path_name_desc
        # Path type.
        self.path_type = path_type
        # Description of the path type.
        self.path_type_desc = path_type_desc
        # Risk level.
        self.risk_level = risk_level
        # Source asset.
        self.src_asset = src_asset

    def validate(self):
        if self.dst_asset:
            self.dst_asset.validate()
        if self.src_asset:
            self.src_asset.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.dst_asset is not None:
            result['DstAsset'] = self.dst_asset.to_map()

        if self.event_id is not None:
            result['EventId'] = self.event_id

        if self.last_time is not None:
            result['LastTime'] = self.last_time

        if self.path_name is not None:
            result['PathName'] = self.path_name

        if self.path_name_desc is not None:
            result['PathNameDesc'] = self.path_name_desc

        if self.path_type is not None:
            result['PathType'] = self.path_type

        if self.path_type_desc is not None:
            result['PathTypeDesc'] = self.path_type_desc

        if self.risk_level is not None:
            result['RiskLevel'] = self.risk_level

        if self.src_asset is not None:
            result['SrcAsset'] = self.src_asset.to_map()

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('DstAsset') is not None:
            temp_model = main_models.ListAttackPathEventResponseBodyAttackPathEventListDstAsset()
            self.dst_asset = temp_model.from_map(m.get('DstAsset'))

        if m.get('EventId') is not None:
            self.event_id = m.get('EventId')

        if m.get('LastTime') is not None:
            self.last_time = m.get('LastTime')

        if m.get('PathName') is not None:
            self.path_name = m.get('PathName')

        if m.get('PathNameDesc') is not None:
            self.path_name_desc = m.get('PathNameDesc')

        if m.get('PathType') is not None:
            self.path_type = m.get('PathType')

        if m.get('PathTypeDesc') is not None:
            self.path_type_desc = m.get('PathTypeDesc')

        if m.get('RiskLevel') is not None:
            self.risk_level = m.get('RiskLevel')

        if m.get('SrcAsset') is not None:
            temp_model = main_models.ListAttackPathEventResponseBodyAttackPathEventListSrcAsset()
            self.src_asset = temp_model.from_map(m.get('SrcAsset'))

        return self

class ListAttackPathEventResponseBodyAttackPathEventListSrcAsset(DaraModel):
    def __init__(
        self,
        asset_sub_type: int = None,
        asset_type: int = None,
        instance_id: str = None,
        instance_name: str = None,
        region_id: str = None,
        sensitive_asset_flag: int = None,
        vendor: int = None,
    ):
        # Subtype of the cloud product asset.
        self.asset_sub_type = asset_sub_type
        # Type of the cloud product asset.
        self.asset_type = asset_type
        # ID of the cloud product asset instance.
        self.instance_id = instance_id
        # Instance name.
        self.instance_name = instance_name
        # ID of the region where the cloud product asset instance is located.
        self.region_id = region_id
        # Sensitive asset flag. Values:
        # - **0**: Non-sensitive asset.
        # - **1**: Sensitive asset.
        self.sensitive_asset_flag = sensitive_asset_flag
        # Vendor of the cloud product asset.
        self.vendor = vendor

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.asset_sub_type is not None:
            result['AssetSubType'] = self.asset_sub_type

        if self.asset_type is not None:
            result['AssetType'] = self.asset_type

        if self.instance_id is not None:
            result['InstanceId'] = self.instance_id

        if self.instance_name is not None:
            result['InstanceName'] = self.instance_name

        if self.region_id is not None:
            result['RegionId'] = self.region_id

        if self.sensitive_asset_flag is not None:
            result['SensitiveAssetFlag'] = self.sensitive_asset_flag

        if self.vendor is not None:
            result['Vendor'] = self.vendor

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('AssetSubType') is not None:
            self.asset_sub_type = m.get('AssetSubType')

        if m.get('AssetType') is not None:
            self.asset_type = m.get('AssetType')

        if m.get('InstanceId') is not None:
            self.instance_id = m.get('InstanceId')

        if m.get('InstanceName') is not None:
            self.instance_name = m.get('InstanceName')

        if m.get('RegionId') is not None:
            self.region_id = m.get('RegionId')

        if m.get('SensitiveAssetFlag') is not None:
            self.sensitive_asset_flag = m.get('SensitiveAssetFlag')

        if m.get('Vendor') is not None:
            self.vendor = m.get('Vendor')

        return self

class ListAttackPathEventResponseBodyAttackPathEventListDstAsset(DaraModel):
    def __init__(
        self,
        asset_sub_type: int = None,
        asset_type: int = None,
        instance_id: str = None,
        instance_name: str = None,
        region_id: str = None,
        sensitive_asset_flag: int = None,
        vendor: int = None,
    ):
        # Subtype of the cloud product asset.
        self.asset_sub_type = asset_sub_type
        # Type of the cloud product asset.
        self.asset_type = asset_type
        # ID of the cloud product asset instance.
        self.instance_id = instance_id
        # Instance name.
        self.instance_name = instance_name
        # ID of the region where the cloud product asset instance is located.
        self.region_id = region_id
        # Sensitive asset flag. Values:
        # - **0**: Non-sensitive asset.
        # - **1**: Sensitive asset.
        self.sensitive_asset_flag = sensitive_asset_flag
        # Vendor of the cloud product asset.
        self.vendor = vendor

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.asset_sub_type is not None:
            result['AssetSubType'] = self.asset_sub_type

        if self.asset_type is not None:
            result['AssetType'] = self.asset_type

        if self.instance_id is not None:
            result['InstanceId'] = self.instance_id

        if self.instance_name is not None:
            result['InstanceName'] = self.instance_name

        if self.region_id is not None:
            result['RegionId'] = self.region_id

        if self.sensitive_asset_flag is not None:
            result['SensitiveAssetFlag'] = self.sensitive_asset_flag

        if self.vendor is not None:
            result['Vendor'] = self.vendor

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('AssetSubType') is not None:
            self.asset_sub_type = m.get('AssetSubType')

        if m.get('AssetType') is not None:
            self.asset_type = m.get('AssetType')

        if m.get('InstanceId') is not None:
            self.instance_id = m.get('InstanceId')

        if m.get('InstanceName') is not None:
            self.instance_name = m.get('InstanceName')

        if m.get('RegionId') is not None:
            self.region_id = m.get('RegionId')

        if m.get('SensitiveAssetFlag') is not None:
            self.sensitive_asset_flag = m.get('SensitiveAssetFlag')

        if m.get('Vendor') is not None:
            self.vendor = m.get('Vendor')

        return self

