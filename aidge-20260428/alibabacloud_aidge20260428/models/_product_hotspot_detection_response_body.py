# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from typing import List, Dict

from alibabacloud_aidge20260428 import models as main_models
from darabonba.model import DaraModel

class ProductHotspotDetectionResponseBody(DaraModel):
    def __init__(
        self,
        code: str = None,
        data: main_models.ProductHotspotDetectionResponseBodyData = None,
        message: str = None,
        request_id: str = None,
        success: bool = None,
    ):
        # The error code.
        self.code = code
        # The product hotzone recognition result.
        self.data = data
        # The error message.
        self.message = message
        # Id of the request
        self.request_id = request_id
        # Indicates whether the call was successful.
        self.success = success

    def validate(self):
        if self.data:
            self.data.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.code is not None:
            result['Code'] = self.code

        if self.data is not None:
            result['Data'] = self.data.to_map()

        if self.message is not None:
            result['Message'] = self.message

        if self.request_id is not None:
            result['RequestId'] = self.request_id

        if self.success is not None:
            result['Success'] = self.success

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Code') is not None:
            self.code = m.get('Code')

        if m.get('Data') is not None:
            temp_model = main_models.ProductHotspotDetectionResponseBodyData()
            self.data = temp_model.from_map(m.get('Data'))

        if m.get('Message') is not None:
            self.message = m.get('Message')

        if m.get('RequestId') is not None:
            self.request_id = m.get('RequestId')

        if m.get('Success') is not None:
            self.success = m.get('Success')

        return self

class ProductHotspotDetectionResponseBodyData(DaraModel):
    def __init__(
        self,
        products: List[main_models.ProductHotspotDetectionResponseBodyDataProducts] = None,
        usage_map: Dict[str, int] = None,
    ):
        # The list of recognized products and their bounding boxes.
        self.products = products
        # The usage information.
        self.usage_map = usage_map

    def validate(self):
        if self.products:
            for v1 in self.products:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['Products'] = []
        if self.products is not None:
            for k1 in self.products:
                result['Products'].append(k1.to_map() if k1 else None)

        if self.usage_map is not None:
            result['UsageMap'] = self.usage_map

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.products = []
        if m.get('Products') is not None:
            for k1 in m.get('Products'):
                temp_model = main_models.ProductHotspotDetectionResponseBodyDataProducts()
                self.products.append(temp_model.from_map(k1))

        if m.get('UsageMap') is not None:
            self.usage_map = m.get('UsageMap')

        return self

class ProductHotspotDetectionResponseBodyDataProducts(DaraModel):
    def __init__(
        self,
        boxes: List[main_models.ProductHotspotDetectionResponseBodyDataProductsBoxes] = None,
        label: str = None,
        product_id: str = None,
    ):
        # The bounding boxes of the product instances.
        self.boxes = boxes
        # The product name.
        self.label = label
        # The product ID generated by the workflow.
        self.product_id = product_id

    def validate(self):
        if self.boxes:
            for v1 in self.boxes:
                 if v1:
                    v1.validate()

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        result['Boxes'] = []
        if self.boxes is not None:
            for k1 in self.boxes:
                result['Boxes'].append(k1.to_map() if k1 else None)

        if self.label is not None:
            result['Label'] = self.label

        if self.product_id is not None:
            result['ProductId'] = self.product_id

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        self.boxes = []
        if m.get('Boxes') is not None:
            for k1 in m.get('Boxes'):
                temp_model = main_models.ProductHotspotDetectionResponseBodyDataProductsBoxes()
                self.boxes.append(temp_model.from_map(k1))

        if m.get('Label') is not None:
            self.label = m.get('Label')

        if m.get('ProductId') is not None:
            self.product_id = m.get('ProductId')

        return self

class ProductHotspotDetectionResponseBodyDataProductsBoxes(DaraModel):
    def __init__(
        self,
        bottom: float = None,
        left: float = None,
        right: float = None,
        top: float = None,
    ):
        # The bottom boundary of the bounding box.
        self.bottom = bottom
        # The left boundary of the bounding box.
        self.left = left
        # The right boundary of the bounding box.
        self.right = right
        # The top boundary of the bounding box.
        self.top = top

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.bottom is not None:
            result['Bottom'] = self.bottom

        if self.left is not None:
            result['Left'] = self.left

        if self.right is not None:
            result['Right'] = self.right

        if self.top is not None:
            result['Top'] = self.top

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('Bottom') is not None:
            self.bottom = m.get('Bottom')

        if m.get('Left') is not None:
            self.left = m.get('Left')

        if m.get('Right') is not None:
            self.right = m.get('Right')

        if m.get('Top') is not None:
            self.top = m.get('Top')

        return self

