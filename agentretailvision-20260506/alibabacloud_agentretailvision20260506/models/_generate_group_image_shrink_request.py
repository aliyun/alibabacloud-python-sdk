# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
from __future__ import annotations

from darabonba.model import DaraModel

class GenerateGroupImageShrinkRequest(DaraModel):
    def __init__(
        self,
        callback_secret: str = None,
        callback_url: str = None,
        group_id: str = None,
        group_type: int = None,
        platform_item_id_list_shrink: str = None,
    ):
        # The key used for callback signature verification.
        self.callback_secret = callback_secret
        # The callback URL of the customer.
        self.callback_url = callback_url
        # The composite image request ID generated by the caller.
        self.group_id = group_id
        # The composite image type. Valid values:
        # - 1: single-SKU multi-image composite.
        # - 2: multi-SKU composite.
        self.group_type = group_type
        # The list of platform item IDs, corresponding to smart_retail_item.platform_item_id.
        self.platform_item_id_list_shrink = platform_item_id_list_shrink

    def validate(self):
        pass

    def to_map(self):
        result = dict()
        _map = super().to_map()
        if _map is not None:
            result = _map
        if self.callback_secret is not None:
            result['CallbackSecret'] = self.callback_secret

        if self.callback_url is not None:
            result['CallbackUrl'] = self.callback_url

        if self.group_id is not None:
            result['GroupId'] = self.group_id

        if self.group_type is not None:
            result['GroupType'] = self.group_type

        if self.platform_item_id_list_shrink is not None:
            result['PlatformItemIdList'] = self.platform_item_id_list_shrink

        return result

    def from_map(self, m: dict = None):
        m = m or dict()
        if m.get('CallbackSecret') is not None:
            self.callback_secret = m.get('CallbackSecret')

        if m.get('CallbackUrl') is not None:
            self.callback_url = m.get('CallbackUrl')

        if m.get('GroupId') is not None:
            self.group_id = m.get('GroupId')

        if m.get('GroupType') is not None:
            self.group_type = m.get('GroupType')

        if m.get('PlatformItemIdList') is not None:
            self.platform_item_id_list_shrink = m.get('PlatformItemIdList')

        return self

